{
  "name": "leon-launcher-tauri",
  "version": "1.0.0",
  "description": "LEON TTS Launcher - Tauri Edition",
  "type": "module",
  "scripts": {
    "frontend:dev": "vite --host 127.0.0.1",
    "frontend:build": "vite build",
    "tauri": "tauri",
    "dev": "tauri dev",
    "build": "tauri build",
    "preview": "npm run build && tauri dev --no-watch"
  },
  "dependencies": {
    "@tauri-apps/api": "^2.1.1"
  },
  "devDependencies": {
    "@tauri-apps/cli": "^2.1.0",
    "vite": "^5.4.0"
  }
}
