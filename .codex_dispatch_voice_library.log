Reading additional input from stdin...
OpenAI Codex v0.132.0
--------
workdir: D:\apiWorkSpace\index-tts2-vLLM
model: pm/gpt-5.5
provider: ming
approval: never
sandbox: workspace-write [workdir, /tmp, $TMPDIR]
reasoning effort: xhigh
reasoning summaries: none
session id: 019e5b12-9d9e-7c91-8f29-89eb33672bd0
--------
user
你是 Codex,与 Claude 协作开发 IndexTTS2 vLLM 的 TAVO 接入。Claude 已在 VLLM-tavo-api 分支完成 Phase 1 (/tts_stream 单段流式)。

## 你的任务
在 VLLM 主分支新建文件 indextts/voice_library.py,提供音色库的纯函数 CRUD。**不要写 FastAPI / HTTP 端点**,Claude 会在 indextts2_api.py 里 import 你的函数包成 HTTP。这样分工不会改同一个文件。

## 工作目录
D:\apiWorkSpace\index-tts2-vLLM (当前 cwd)
确认在 VLLM 分支:git branch --show-current 应该输出 VLLM 或 master,如果是 VLLM-tavo-api 立即切回 VLLM(那是 Claude 的分支不要动)

## 设计参考
V26 整合包 webui.py 的 212-308 行有完整参考实现可以搬:
路径:D:\apiWorkSpace\Index-TTS-V26\Index-TTS-V26\dist\index-tts-windows-cu128-deepspeed\app\webui.py
函数:_safe_voice_name / voice_lib_list / voice_lib_path / _voice_lib_save / on_delete_from_library
**只搬纯 Python 逻辑,不要 import gradio,不要 gr.Warning/gr.Info/gr.update。**

## 接口规格(必须实现这 6 个函数)

VOICE_LIB_DIR = 'prompts/library'  # 相对 cwd
VOICE_LIB_EXTS = ('.wav', '.mp3', '.flac', '.ogg', '.m4a')

def safe_voice_name(name: str) -> str:
    '''清洗为安全文件名,去 Windows 非法字符 \/:*?\"<>| 和控制字符,截到 60 字符'''

def list_voices() -> list[dict]:
    '''返回 [{"name": "voice_a", "path": "prompts/library/voice_a.wav", "ext": ".wav", "size_bytes": 12345}, ...] 按 name 排序'''

def get_voice_path(name: str) -> Optional[str]:
    '''音色名找绝对/相对文件路径,不存在返回 None'''

def save_voice(audio_bytes: bytes, name: str, ext: str = '.wav') -> str:
    '''保存上传字节,自动删除同名旧文件(可能扩展不同),返回最终文件路径。ext 不在 VOICE_LIB_EXTS 时强制 .wav'''

def save_voice_from_path(source_path: str, name: str) -> str:
    '''从本地路径复制保存,扩展从源文件检测。同名旧文件删除。'''

def delete_voice(name: str) -> bool:
    '''删除返回 True,文件不存在返回 False。不抛异常。'''

## 约束
- 只用 Python stdlib (os, re, shutil, pathlib, typing)。**禁止 numpy/torch/gradio 等额外依赖**
- 没有数据库,纯文件
- **绝对不要碰**:indextts2_api.py / webui.py / indextts/infer_vllm_v2.py / indextts/s2mel/modules/flow_matching.py
- 每个函数加简短 docstring

## 完成后
1. 检查语法:D:\apiWorkSpace\index-tts2-vLLM\indextts2runtime\python.exe -m py_compile indextts/voice_library.py
2. git add indextts/voice_library.py
3. git commit -m '[codex] Add voice_library module for Phase 2/3 voice CRUD'
4. git push userrepo VLLM (用 userrepo,不是 origin,origin 有 GitHub 重定向问题推不动)
5. 在 stdout 输出 1-2 行中文:完成情况 + commit SHA

干完即退出,不要等任何确认。
codex
我先确认分支和工作区状态，再读取 V26 参考实现，只在 `indextts/voice_library.py` 新增代码。
exec
"C:\\Program Files\\PowerShell\\7\\pwsh.exe" -Command 'git branch --show-current' in D:\apiWorkSpace\index-tts2-vLLM
exec
"C:\\Program Files\\PowerShell\\7\\pwsh.exe" -Command 'git status --short' in D:\apiWorkSpace\index-tts2-vLLM
exec
"C:\\Program Files\\PowerShell\\7\\pwsh.exe" -Command "Test-Path -LiteralPath 'D:\\apiWorkSpace\\Index-TTS-V26\\Index-TTS-V26\\dist\\index-tts-windows-cu128-deepspeed\\app\\webui.py'" in D:\apiWorkSpace\index-tts2-vLLM
 succeeded in 17774ms:
VLLM

 succeeded in 17802ms:
True

 succeeded in 17810ms:
?? .claude/
?? .codex_dispatch_voice_library.log
?? "\351\237\263\350\211\262\345\217\202\350\200\203\351\237\263\351\242\221/"

codex
分支已经是 `VLLM`，工作区里有一些未跟踪文件，我会保持不动。现在只看参考函数和目标目录结构。
exec
"C:\\Program Files\\PowerShell\\7\\pwsh.exe" -Command '$p='"'D:\\apiWorkSpace\\Index-TTS-V26\\Index-TTS-V26\\dist\\index-tts-windows-cu128-deepspeed\\app\\webui.py'; "'$lines=Get-Content -LiteralPath $p; $start=212; $end=308; for($i=$start; $i -le $end; $i++){ if($i -le $lines.Length){ '"'{0,4}: {1}' -f "'$i, $lines[$i-1] } }' in D:\apiWorkSpace\index-tts2-vLLM
exec
"C:\\Program Files\\PowerShell\\7\\pwsh.exe" -Command 'Get-ChildItem -LiteralPath indextts | Select-Object -ExpandProperty Name' in D:\apiWorkSpace\index-tts2-vLLM
exec
"C:\\Program Files\\PowerShell\\7\\pwsh.exe" -Command "Test-Path -LiteralPath indextts\\voice_library.py" in D:\apiWorkSpace\index-tts2-vLLM
 succeeded in 13378ms:
False

 succeeded in 13403ms:
__pycache__
BigVGAN
gpt
s2mel
utils
vqvae
__init__.py
cli.py
infer_v2.py
infer_vllm_v2.py
infer.py

 succeeded in 13563ms:
 212: # Voice Library (shared between speaker-ref and emotion-ref slots)
 213: # storage: ./prompts/library/<name>.<ext>
 214: # ------------------------------------------------------------------
 215: VOICE_LIB_DIR = os.path.join("prompts", "library")
 216: VOICE_LIB_EXTS = (".wav", ".mp3", ".flac", ".ogg", ".m4a")
 217: os.makedirs(VOICE_LIB_DIR, exist_ok=True)
 218: 
 219: 
 220: def _safe_voice_name(name: str) -> str:
 221:     name = (name or "").strip()
 222:     name = re.sub(r'[\\/:*?"<>|\x00-\x1f]', '', name)
 223:     return name[:60]
 224: 
 225: 
 226: def voice_lib_list():
 227:     if not os.path.isdir(VOICE_LIB_DIR):
 228:         return []
 229:     items = []
 230:     for fn in sorted(os.listdir(VOICE_LIB_DIR)):
 231:         if fn.lower().endswith(VOICE_LIB_EXTS):
 232:             items.append(os.path.splitext(fn)[0])
 233:     return items
 234: 
 235: 
 236: def voice_lib_path(name: str):
 237:     safe = _safe_voice_name(name)
 238:     if not safe:
 239:         return None
 240:     for ext in VOICE_LIB_EXTS:
 241:         p = os.path.join(VOICE_LIB_DIR, safe + ext)
 242:         if os.path.exists(p):
 243:             return p
 244:     return None
 245: 
 246: 
 247: def _voice_lib_save(audio_path, name):
 248:     """Returns (saved_name_or_None, choices_list). Emits toast."""
 249:     if not audio_path:
 250:         gr.Warning(i18n("请先上传或录制一段音频"))
 251:         return None, voice_lib_list()
 252:     safe = _safe_voice_name(name)
 253:     if not safe:
 254:         gr.Warning(i18n("请填写音色名称"))
 255:         return None, voice_lib_list()
 256:     ext = os.path.splitext(audio_path)[1].lower() or ".wav"
 257:     if ext not in VOICE_LIB_EXTS:
 258:         ext = ".wav"
 259:     dst = os.path.join(VOICE_LIB_DIR, safe + ext)
 260:     try:
 261:         for old_ext in VOICE_LIB_EXTS:
 262:             old = os.path.join(VOICE_LIB_DIR, safe + old_ext)
 263:             if old_ext != ext and os.path.exists(old):
 264:                 os.remove(old)
 265:         shutil.copy2(audio_path, dst)
 266:         gr.Info(i18n("音色已保存：") + safe, duration=2)
 267:     except Exception as e:
 268:         gr.Error(i18n("保存失败：") + str(e))
 269:         return None, voice_lib_list()
 270:     return safe, voice_lib_list()
 271: 
 272: 
 273: def on_save_to_library(audio_path, name):
 274:     saved, choices = _voice_lib_save(audio_path, name)
 275:     value = saved if saved else gr.update()
 276:     return gr.update(choices=choices, value=value), gr.update(choices=choices)
 277: 
 278: 
 279: def on_load_from_library(name):
 280:     if not name:
 281:         gr.Warning(i18n("请先从音色库选择一个音色"))
 282:         return gr.update()
 283:     p = voice_lib_path(name)
 284:     if not p:
 285:         gr.Warning(i18n("音色文件不存在，请刷新列表"))
 286:         return gr.update()
 287:     return gr.update(value=p)
 288: 
 289: 
 290: def on_delete_from_library(name):
 291:     if not name:
 292:         gr.Warning(i18n("请先选择要删除的音色"))
 293:         choices = voice_lib_list()
 294:         return gr.update(choices=choices), gr.update(choices=choices)
 295:     p = voice_lib_path(name)
 296:     if p and os.path.exists(p):
 297:         try:
 298:             os.remove(p)
 299:             gr.Info(i18n("已删除：") + name, duration=2)
 300:         except Exception as e:
 301:             gr.Error(i18n("删除失败：") + str(e))
 302:     choices = voice_lib_list()
 303:     return gr.update(choices=choices, value=None), gr.update(choices=choices, value=None)
 304: 
 305: 
 306: def on_refresh_library():
 307:     choices = voice_lib_list()
 308:     return gr.update(choices=choices), gr.update(choices=choices)

codex
参考实现的核心就是文件名清洗、按扩展名查找、保存前清理同名旧文件。我会把它改成无 UI 依赖的纯 stdlib 模块，并补齐字节保存、列表元数据和静默删除语义。
codex
我将新增模块文件，常量保持为相对路径 `prompts/library`；返回路径用正斜杠形式，文件操作仍走 `pathlib`，这样 Windows 下也能正常处理。
apply patch
patch: completed
D:\apiWorkSpace\index-tts2-vLLM\indextts\voice_library.py
diff --git a/indextts/voice_library.py b/indextts/voice_library.py
new file mode 100644
index 0000000000000000000000000000000000000000..946fc3244e2d1961ce96f7aac3db7cd891a532a8
--- /dev/null
+++ b/indextts/voice_library.py
@@ -0,0 +1,161 @@
+import re
+import shutil
+from pathlib import Path
+from typing import Optional
+
+
+VOICE_LIB_DIR = "prompts/library"
+VOICE_LIB_EXTS = (".wav", ".mp3", ".flac", ".ogg", ".m4a")
+
+_INVALID_NAME_CHARS = re.compile(r'[\\/:*?"<>|\x00-\x1f]')
+
+
+def safe_voice_name(name: str) -> str:
+    """Return a Windows-safe voice filename stem capped at 60 chars."""
+    cleaned = _INVALID_NAME_CHARS.sub("", (name or "").strip())
+    return cleaned[:60]
+
+
+def list_voices() -> list[dict]:
+    """List saved voices with name, relative path, extension, and byte size."""
+    library_dir = Path(VOICE_LIB_DIR)
+    if not library_dir.is_dir():
+        return []
+
+    items = []
+    for path in library_dir.iterdir():
+        if not path.is_file():
+            continue
+
+        ext = path.suffix.lower()
+        if ext not in VOICE_LIB_EXTS:
+            continue
+
+        try:
+            size_bytes = path.stat().st_size
+        except OSError:
+            continue
+
+        items.append(
+            {
+                "name": path.stem,
+                "path": _format_voice_path(path),
+                "ext": ext,
+                "size_bytes": size_bytes,
+            }
+        )
+
+    items.sort(key=lambda item: (item["name"].lower(), item["name"], item["ext"]))
+    return items
+
+
+def get_voice_path(name: str) -> Optional[str]:
+    """Return a saved voice path by name, or None when it does not exist."""
+    safe = safe_voice_name(name)
+    if not safe:
+        return None
+
+    path = _find_voice_path(safe)
+    if path is None:
+        return None
+    return _format_voice_path(path)
+
+
+def save_voice(audio_bytes: bytes, name: str, ext: str = ".wav") -> str:
+    """Save uploaded audio bytes and return the final voice file path."""
+    safe = safe_voice_name(name)
+    if not safe:
+        raise ValueError("voice name is empty")
+
+    normalized_ext = _normalize_voice_ext(ext)
+    dst = _voice_path(safe, normalized_ext)
+    dst.parent.mkdir(parents=True, exist_ok=True)
+    _delete_existing_voice_files(safe, keep=dst)
+
+    with dst.open("wb") as file:
+        file.write(audio_bytes)
+
+    return _format_voice_path(dst)
+
+
+def save_voice_from_path(source_path: str, name: str) -> str:
+    """Copy a local audio file into the voice library and return its path."""
+    safe = safe_voice_name(name)
+    if not safe:
+        raise ValueError("voice name is empty")
+
+    source = Path(source_path)
+    normalized_ext = _normalize_voice_ext(source.suffix)
+    dst = _voice_path(safe, normalized_ext)
+    dst.parent.mkdir(parents=True, exist_ok=True)
+    _delete_existing_voice_files(safe, keep=dst)
+
+    if source.resolve(strict=False) != dst.resolve(strict=False):
+        shutil.copy2(source, dst)
+
+    return _format_voice_path(dst)
+
+
+def delete_voice(name: str) -> bool:
+    """Delete a saved voice by name, returning False when absent or failed."""
+    safe = safe_voice_name(name)
+    if not safe:
+        return False
+
+    path = _find_voice_path(safe)
+    if path is None:
+        return False
+
+    try:
+        path.unlink()
+    except OSError:
+        return False
+    return True
+
+
+def _normalize_voice_ext(ext: str) -> str:
+    ext = (ext or ".wav").lower()
+    if not ext.startswith("."):
+        ext = "." + ext
+    if ext not in VOICE_LIB_EXTS:
+        return ".wav"
+    return ext
+
+
+def _voice_path(name: str, ext: str) -> Path:
+    return Path(VOICE_LIB_DIR) / f"{name}{ext}"
+
+
+def _find_voice_path(safe_name: str) -> Optional[Path]:
+    library_dir = Path(VOICE_LIB_DIR)
+    for ext in VOICE_LIB_EXTS:
+        path = library_dir / f"{safe_name}{ext}"
+        if path.is_file():
+            return path
+
+    if not library_dir.is_dir():
+        return None
+
+    for ext in VOICE_LIB_EXTS:
+        target_name = f"{safe_name}{ext}".lower()
+        for path in library_dir.iterdir():
+            if path.is_file() and path.name.lower() == target_name:
+                return path
+
+    return None
+
+
+def _delete_existing_voice_files(safe_name: str, keep: Optional[Path] = None) -> None:
+    keep_resolved = keep.resolve(strict=False) if keep is not None else None
+    for ext in VOICE_LIB_EXTS:
+        path = _voice_path(safe_name, ext)
+        if keep_resolved is not None and path.resolve(strict=False) == keep_resolved:
+            continue
+        try:
+            path.unlink()
+        except FileNotFoundError:
+            continue
+
+
+def _format_voice_path(path: Path) -> str:
+    return path.as_posix()

diff --git a/indextts/voice_library.py b/indextts/voice_library.py
new file mode 100644
index 0000000000000000000000000000000000000000..946fc3244e2d1961ce96f7aac3db7cd891a532a8
--- /dev/null
+++ b/indextts/voice_library.py
@@ -0,0 +1,161 @@
+import re
+import shutil
+from pathlib import Path
+from typing import Optional
+
+
+VOICE_LIB_DIR = "prompts/library"
+VOICE_LIB_EXTS = (".wav", ".mp3", ".flac", ".ogg", ".m4a")
+
+_INVALID_NAME_CHARS = re.compile(r'[\\/:*?"<>|\x00-\x1f]')
+
+
+def safe_voice_name(name: str) -> str:
+    """Return a Windows-safe voice filename stem capped at 60 chars."""
+    cleaned = _INVALID_NAME_CHARS.sub("", (name or "").strip())
+    return cleaned[:60]
+
+
+def list_voices() -> list[dict]:
+    """List saved voices with name, relative path, extension, and byte size."""
+    library_dir = Path(VOICE_LIB_DIR)
+    if not library_dir.is_dir():
+        return []
+
+    items = []
+    for path in library_dir.iterdir():
+        if not path.is_file():
+            continue
+
+        ext = path.suffix.lower()
+        if ext not in VOICE_LIB_EXTS:
+            continue
+
+        try:
+            size_bytes = path.stat().st_size
+        except OSError:
+            continue
+
+        items.append(
+            {
+                "name": path.stem,
+                "path": _format_voice_path(path),
+                "ext": ext,
+                "size_bytes": size_bytes,
+            }
+        )
+
+    items.sort(key=lambda item: (item["name"].lower(), item["name"], item["ext"]))
+    return items
+
+
+def get_voice_path(name: str) -> Optional[str]:
+    """Return a saved voice path by name, or None when it does not exist."""
+    safe = safe_voice_name(name)
+    if not safe:
+        return None
+
+    path = _find_voice_path(safe)
+    if path is None:
+        return None
+    return _format_voice_path(path)
+
+
+def save_voice(audio_bytes: bytes, name: str, ext: str = ".wav") -> str:
+    """Save uploaded audio bytes and return the final voice file path."""
+    safe = safe_voice_name(name)
+    if not safe:
+        raise ValueError("voice name is empty")
+
+    normalized_ext = _normalize_voice_ext(ext)
+    dst = _voice_path(safe, normalized_ext)
+    dst.parent.mkdir(parents=True, exist_ok=True)
+    _delete_existing_voice_files(safe, keep=dst)
+
+    with dst.open("wb") as file:
+        file.write(audio_bytes)
+
+    return _format_voice_path(dst)
+
+
+def save_voice_from_path(source_path: str, name: str) -> str:
+    """Copy a local audio file into the voice library and return its path."""
+    safe = safe_voice_name(name)
+    if not safe:
+        raise ValueError("voice name is empty")
+
+    source = Path(source_path)
+    normalized_ext = _normalize_voice_ext(source.suffix)
+    dst = _voice_path(safe, normalized_ext)
+    dst.parent.mkdir(parents=True, exist_ok=True)
+    _delete_existing_voice_files(safe, keep=dst)
+
+    if source.resolve(strict=False) != dst.resolve(strict=False):
+        shutil.copy2(source, dst)
+
+    return _format_voice_path(dst)
+
+
+def delete_voice(name: str) -> bool:
+    """Delete a saved voice by name, returning False when absent or failed."""
+    safe = safe_voice_name(name)
+    if not safe:
+        return False
+
+    path = _find_voice_path(safe)
+    if path is None:
+        return False
+
+    try:
+        path.unlink()
+    except OSError:
+        return False
+    return True
+
+
+def _normalize_voice_ext(ext: str) -> str:
+    ext = (ext or ".wav").lower()
+    if not ext.startswith("."):
+        ext = "." + ext
+    if ext not in VOICE_LIB_EXTS:
+        return ".wav"
+    return ext
+
+
+def _voice_path(name: str, ext: str) -> Path:
+    return Path(VOICE_LIB_DIR) / f"{name}{ext}"
+
+
+def _find_voice_path(safe_name: str) -> Optional[Path]:
+    library_dir = Path(VOICE_LIB_DIR)
+    for ext in VOICE_LIB_EXTS:
+        path = library_dir / f"{safe_name}{ext}"
+        if path.is_file():
+            return path
+
+    if not library_dir.is_dir():
+        return None
+
+    for ext in VOICE_LIB_EXTS:
+        target_name = f"{safe_name}{ext}".lower()
+        for path in library_dir.iterdir():
+            if path.is_file() and path.name.lower() == target_name:
+                return path
+
+    return None
+
+
+def _delete_existing_voice_files(safe_name: str, keep: Optional[Path] = None) -> None:
+    keep_resolved = keep.resolve(strict=False) if keep is not None else None
+    for ext in VOICE_LIB_EXTS:
+        path = _voice_path(safe_name, ext)
+        if keep_resolved is not None and path.resolve(strict=False) == keep_resolved:
+            continue
+        try:
+            path.unlink()
+        except FileNotFoundError:
+            continue
+
+
+def _format_voice_path(path: Path) -> str:
+    return path.as_posix()

codex
新增文件已完成初稿。我注意到本地路径复制在源文件不存在且刚好等于目标路径时需要显式报错，先补上这个边界再编译。
apply patch
patch: completed
D:\apiWorkSpace\index-tts2-vLLM\indextts\voice_library.py
diff --git a/indextts/voice_library.py b/indextts/voice_library.py
new file mode 100644
index 0000000000000000000000000000000000000000..4ee5d9b8f5f16a07feb288be39cfcdcdd438b696
--- /dev/null
+++ b/indextts/voice_library.py
@@ -0,0 +1,164 @@
+import re
+import shutil
+from pathlib import Path
+from typing import Optional
+
+
+VOICE_LIB_DIR = "prompts/library"
+VOICE_LIB_EXTS = (".wav", ".mp3", ".flac", ".ogg", ".m4a")
+
+_INVALID_NAME_CHARS = re.compile(r'[\\/:*?"<>|\x00-\x1f]')
+
+
+def safe_voice_name(name: str) -> str:
+    """Return a Windows-safe voice filename stem capped at 60 chars."""
+    cleaned = _INVALID_NAME_CHARS.sub("", (name or "").strip())
+    return cleaned[:60]
+
+
+def list_voices() -> list[dict]:
+    """List saved voices with name, relative path, extension, and byte size."""
+    library_dir = Path(VOICE_LIB_DIR)
+    if not library_dir.is_dir():
+        return []
+
+    items = []
+    for path in library_dir.iterdir():
+        if not path.is_file():
+            continue
+
+        ext = path.suffix.lower()
+        if ext not in VOICE_LIB_EXTS:
+            continue
+
+        try:
+            size_bytes = path.stat().st_size
+        except OSError:
+            continue
+
+        items.append(
+            {
+                "name": path.stem,
+                "path": _format_voice_path(path),
+                "ext": ext,
+                "size_bytes": size_bytes,
+            }
+        )
+
+    items.sort(key=lambda item: (item["name"].lower(), item["name"], item["ext"]))
+    return items
+
+
+def get_voice_path(name: str) -> Optional[str]:
+    """Return a saved voice path by name, or None when it does not exist."""
+    safe = safe_voice_name(name)
+    if not safe:
+        return None
+
+    path = _find_voice_path(safe)
+    if path is None:
+        return None
+    return _format_voice_path(path)
+
+
+def save_voice(audio_bytes: bytes, name: str, ext: str = ".wav") -> str:
+    """Save uploaded audio bytes and return the final voice file path."""
+    safe = safe_voice_name(name)
+    if not safe:
+        raise ValueError("voice name is empty")
+
+    normalized_ext = _normalize_voice_ext(ext)
+    dst = _voice_path(safe, normalized_ext)
+    dst.parent.mkdir(parents=True, exist_ok=True)
+    _delete_existing_voice_files(safe, keep=dst)
+
+    with dst.open("wb") as file:
+        file.write(audio_bytes)
+
+    return _format_voice_path(dst)
+
+
+def save_voice_from_path(source_path: str, name: str) -> str:
+    """Copy a local audio file into the voice library and return its path."""
+    safe = safe_voice_name(name)
+    if not safe:
+        raise ValueError("voice name is empty")
+
+    source = Path(source_path)
+    if not source.is_file():
+        raise FileNotFoundError(source_path)
+
+    normalized_ext = _normalize_voice_ext(source.suffix)
+    dst = _voice_path(safe, normalized_ext)
+    dst.parent.mkdir(parents=True, exist_ok=True)
+    _delete_existing_voice_files(safe, keep=dst)
+
+    if source.resolve(strict=False) != dst.resolve(strict=False):
+        shutil.copy2(source, dst)
+
+    return _format_voice_path(dst)
+
+
+def delete_voice(name: str) -> bool:
+    """Delete a saved voice by name, returning False when absent or failed."""
+    safe = safe_voice_name(name)
+    if not safe:
+        return False
+
+    path = _find_voice_path(safe)
+    if path is None:
+        return False
+
+    try:
+        path.unlink()
+    except OSError:
+        return False
+    return True
+
+
+def _normalize_voice_ext(ext: str) -> str:
+    ext = (ext or ".wav").lower()
+    if not ext.startswith("."):
+        ext = "." + ext
+    if ext not in VOICE_LIB_EXTS:
+        return ".wav"
+    return ext
+
+
+def _voice_path(name: str, ext: str) -> Path:
+    return Path(VOICE_LIB_DIR) / f"{name}{ext}"
+
+
+def _find_voice_path(safe_name: str) -> Optional[Path]:
+    library_dir = Path(VOICE_LIB_DIR)
+    for ext in VOICE_LIB_EXTS:
+        path = library_dir / f"{safe_name}{ext}"
+        if path.is_file():
+            return path
+
+    if not library_dir.is_dir():
+        return None
+
+    for ext in VOICE_LIB_EXTS:
+        target_name = f"{safe_name}{ext}".lower()
+        for path in library_dir.iterdir():
+            if path.is_file() and path.name.lower() == target_name:
+                return path
+
+    return None
+
+
+def _delete_existing_voice_files(safe_name: str, keep: Optional[Path] = None) -> None:
+    keep_resolved = keep.resolve(strict=False) if keep is not None else None
+    for ext in VOICE_LIB_EXTS:
+        path = _voice_path(safe_name, ext)
+        if keep_resolved is not None and path.resolve(strict=False) == keep_resolved:
+            continue
+        try:
+            path.unlink()
+        except FileNotFoundError:
+            continue
+
+
+def _format_voice_path(path: Path) -> str:
+    return path.as_posix()

diff --git a/indextts/voice_library.py b/indextts/voice_library.py
new file mode 100644
index 0000000000000000000000000000000000000000..4ee5d9b8f5f16a07feb288be39cfcdcdd438b696
--- /dev/null
+++ b/indextts/voice_library.py
@@ -0,0 +1,164 @@
+import re
+import shutil
+from pathlib import Path
+from typing import Optional
+
+
+VOICE_LIB_DIR = "prompts/library"
+VOICE_LIB_EXTS = (".wav", ".mp3", ".flac", ".ogg", ".m4a")
+
+_INVALID_NAME_CHARS = re.compile(r'[\\/:*?"<>|\x00-\x1f]')
+
+
+def safe_voice_name(name: str) -> str:
+    """Return a Windows-safe voice filename stem capped at 60 chars."""
+    cleaned = _INVALID_NAME_CHARS.sub("", (name or "").strip())
+    return cleaned[:60]
+
+
+def list_voices() -> list[dict]:
+    """List saved voices with name, relative path, extension, and byte size."""
+    library_dir = Path(VOICE_LIB_DIR)
+    if not library_dir.is_dir():
+        return []
+
+    items = []
+    for path in library_dir.iterdir():
+        if not path.is_file():
+            continue
+
+        ext = path.suffix.lower()
+        if ext not in VOICE_LIB_EXTS:
+            continue
+
+        try:
+            size_bytes = path.stat().st_size
+        except OSError:
+            continue
+
+        items.append(
+            {
+                "name": path.stem,
+                "path": _format_voice_path(path),
+                "ext": ext,
+                "size_bytes": size_bytes,
+            }
+        )
+
+    items.sort(key=lambda item: (item["name"].lower(), item["name"], item["ext"]))
+    return items
+
+
+def get_voice_path(name: str) -> Optional[str]:
+    """Return a saved voice path by name, or None when it does not exist."""
+    safe = safe_voice_name(name)
+    if not safe:
+        return None
+
+    path = _find_voice_path(safe)
+    if path is None:
+        return None
+    return _format_voice_path(path)
+
+
+def save_voice(audio_bytes: bytes, name: str, ext: str = ".wav") -> str:
+    """Save uploaded audio bytes and return the final voice file path."""
+    safe = safe_voice_name(name)
+    if not safe:
+        raise ValueError("voice name is empty")
+
+    normalized_ext = _normalize_voice_ext(ext)
+    dst = _voice_path(safe, normalized_ext)
+    dst.parent.mkdir(parents=True, exist_ok=True)
+    _delete_existing_voice_files(safe, keep=dst)
+
+    with dst.open("wb") as file:
+        file.write(audio_bytes)
+
+    return _format_voice_path(dst)
+
+
+def save_voice_from_path(source_path: str, name: str) -> str:
+    """Copy a local audio file into the voice library and return its path."""
+    safe = safe_voice_name(name)
+    if not safe:
+        raise ValueError("voice name is empty")
+
+    source = Path(source_path)
+    if not source.is_file():
+        raise FileNotFoundError(source_path)
+
+    normalized_ext = _normalize_voice_ext(source.suffix)
+    dst = _voice_path(safe, normalized_ext)
+    dst.parent.mkdir(parents=True, exist_ok=True)
+    _delete_existing_voice_files(safe, keep=dst)
+
+    if source.resolve(strict=False) != dst.resolve(strict=False):
+        shutil.copy2(source, dst)
+
+    return _format_voice_path(dst)
+
+
+def delete_voice(name: str) -> bool:
+    """Delete a saved voice by name, returning False when absent or failed."""
+    safe = safe_voice_name(name)
+    if not safe:
+        return False
+
+    path = _find_voice_path(safe)
+    if path is None:
+        return False
+
+    try:
+        path.unlink()
+    except OSError:
+        return False
+    return True
+
+
+def _normalize_voice_ext(ext: str) -> str:
+    ext = (ext or ".wav").lower()
+    if not ext.startswith("."):
+        ext = "." + ext
+    if ext not in VOICE_LIB_EXTS:
+        return ".wav"
+    return ext
+
+
+def _voice_path(name: str, ext: str) -> Path:
+    return Path(VOICE_LIB_DIR) / f"{name}{ext}"
+
+
+def _find_voice_path(safe_name: str) -> Optional[Path]:
+    library_dir = Path(VOICE_LIB_DIR)
+    for ext in VOICE_LIB_EXTS:
+        path = library_dir / f"{safe_name}{ext}"
+        if path.is_file():
+            return path
+
+    if not library_dir.is_dir():
+        return None
+
+    for ext in VOICE_LIB_EXTS:
+        target_name = f"{safe_name}{ext}".lower()
+        for path in library_dir.iterdir():
+            if path.is_file() and path.name.lower() == target_name:
+                return path
+
+    return None
+
+
+def _delete_existing_voice_files(safe_name: str, keep: Optional[Path] = None) -> None:
+    keep_resolved = keep.resolve(strict=False) if keep is not None else None
+    for ext in VOICE_LIB_EXTS:
+        path = _voice_path(safe_name, ext)
+        if keep_resolved is not None and path.resolve(strict=False) == keep_resolved:
+            continue
+        try:
+            path.unlink()
+        except FileNotFoundError:
+            continue
+
+
+def _format_voice_path(path: Path) -> str:
+    return path.as_posix()

codex
现在运行指定解释器做语法检查，然后只暂存这个新模块。
exec
"C:\\Program Files\\PowerShell\\7\\pwsh.exe" -Command "D:\\apiWorkSpace\\index-tts2-vLLM\\indextts2runtime\\python.exe -m py_compile indextts/voice_library.py" in D:\apiWorkSpace\index-tts2-vLLM
 succeeded in 605ms:
diff --git a/indextts/voice_library.py b/indextts/voice_library.py
new file mode 100644
index 0000000000000000000000000000000000000000..4ee5d9b8f5f16a07feb288be39cfcdcdd438b696
--- /dev/null
+++ b/indextts/voice_library.py
@@ -0,0 +1,164 @@
+import re
+import shutil
+from pathlib import Path
+from typing import Optional
+
+
+VOICE_LIB_DIR = "prompts/library"
+VOICE_LIB_EXTS = (".wav", ".mp3", ".flac", ".ogg", ".m4a")
+
+_INVALID_NAME_CHARS = re.compile(r'[\\/:*?"<>|\x00-\x1f]')
+
+
+def safe_voice_name(name: str) -> str:
+    """Return a Windows-safe voice filename stem capped at 60 chars."""
+    cleaned = _INVALID_NAME_CHARS.sub("", (name or "").strip())
+    return cleaned[:60]
+
+
+def list_voices() -> list[dict]:
+    """List saved voices with name, relative path, extension, and byte size."""
+    library_dir = Path(VOICE_LIB_DIR)
+    if not library_dir.is_dir():
+        return []
+
+    items = []
+    for path in library_dir.iterdir():
+        if not path.is_file():
+            continue
+
+        ext = path.suffix.lower()
+        if ext not in VOICE_LIB_EXTS:
+            continue
+
+        try:
+            size_bytes = path.stat().st_size
+        except OSError:
+            continue
+
+        items.append(
+            {
+                "name": path.stem,
+                "path": _format_voice_path(path),
+                "ext": ext,
+                "size_bytes": size_bytes,
+            }
+        )
+
+    items.sort(key=lambda item: (item["name"].lower(), item["name"], item["ext"]))
+    return items
+
+
+def get_voice_path(name: str) -> Optional[str]:
+    """Return a saved voice path by name, or None when it does not exist."""
+    safe = safe_voice_name(name)
+    if not safe:
+        return None
+
+    path = _find_voice_path(safe)
+    if path is None:
+        return None
+    return _format_voice_path(path)
+
+
+def save_voice(audio_bytes: bytes, name: str, ext: str = ".wav") -> str:
+    """Save uploaded audio bytes and return the final voice file path."""
+    safe = safe_voice_name(name)
+    if not safe:
+        raise ValueError("voice name is empty")
+
+    normalized_ext = _normalize_voice_ext(ext)
+    dst = _voice_path(safe, normalized_ext)
+    dst.parent.mkdir(parents=True, exist_ok=True)
+    _delete_existing_voice_files(safe, keep=dst)
+
+    with dst.open("wb") as file:
+        file.write(audio_bytes)
+
+    return _format_voice_path(dst)
+
+
+def save_voice_from_path(source_path: str, name: str) -> str:
+    """Copy a local audio file into the voice library and return its path."""
+    safe = safe_voice_name(name)
+    if not safe:
+        raise ValueError("voice name is empty")
+
+    source = Path(source_path)
+    if not source.is_file():
+        raise FileNotFoundError(source_path)
+
+    normalized_ext = _normalize_voice_ext(source.suffix)
+    dst = _voice_path(safe, normalized_ext)
+    dst.parent.mkdir(parents=True, exist_ok=True)
+    _delete_existing_voice_files(safe, keep=dst)
+
+    if source.resolve(strict=False) != dst.resolve(strict=False):
+        shutil.copy2(source, dst)
+
+    return _format_voice_path(dst)
+
+
+def delete_voice(name: str) -> bool:
+    """Delete a saved voice by name, returning False when absent or failed."""
+    safe = safe_voice_name(name)
+    if not safe:
+        return False
+
+    path = _find_voice_path(safe)
+    if path is None:
+        return False
+
+    try:
+        path.unlink()
+    except OSError:
+        return False
+    return True
+
+
+def _normalize_voice_ext(ext: str) -> str:
+    ext = (ext or ".wav").lower()
+    if not ext.startswith("."):
+        ext = "." + ext
+    if ext not in VOICE_LIB_EXTS:
+        return ".wav"
+    return ext
+
+
+def _voice_path(name: str, ext: str) -> Path:
+    return Path(VOICE_LIB_DIR) / f"{name}{ext}"
+
+
+def _find_voice_path(safe_name: str) -> Optional[Path]:
+    library_dir = Path(VOICE_LIB_DIR)
+    for ext in VOICE_LIB_EXTS:
+        path = library_dir / f"{safe_name}{ext}"
+        if path.is_file():
+            return path
+
+    if not library_dir.is_dir():
+        return None
+
+    for ext in VOICE_LIB_EXTS:
+        target_name = f"{safe_name}{ext}".lower()
+        for path in library_dir.iterdir():
+            if path.is_file() and path.name.lower() == target_name:
+                return path
+
+    return None
+
+
+def _delete_existing_voice_files(safe_name: str, keep: Optional[Path] = None) -> None:
+    keep_resolved = keep.resolve(strict=False) if keep is not None else None
+    for ext in VOICE_LIB_EXTS:
+        path = _voice_path(safe_name, ext)
+        if keep_resolved is not None and path.resolve(strict=False) == keep_resolved:
+            continue
+        try:
+            path.unlink()
+        except FileNotFoundError:
+            continue
+
+
+def _format_voice_path(path: Path) -> str:
+    return path.as_posix()

exec
"C:\\Program Files\\PowerShell\\7\\pwsh.exe" -Command 'git status --short' in D:\apiWorkSpace\index-tts2-vLLM
exec
"C:\\Program Files\\PowerShell\\7\\pwsh.exe" -Command 'git diff -- indextts/voice_library.py' in D:\apiWorkSpace\index-tts2-vLLM
 succeeded in 486ms:
?? .claude/
?? .codex_dispatch_voice_library.log
?? indextts/voice_library.py
?? "\351\237\263\350\211\262\345\217\202\350\200\203\351\237\263\351\242\221/"

 succeeded in 427ms:
diff --git a/indextts/voice_library.py b/indextts/voice_library.py
new file mode 100644
index 0000000000000000000000000000000000000000..4ee5d9b8f5f16a07feb288be39cfcdcdd438b696
--- /dev/null
+++ b/indextts/voice_library.py
@@ -0,0 +1,164 @@
+import re
+import shutil
+from pathlib import Path
+from typing import Optional
+
+
+VOICE_LIB_DIR = "prompts/library"
+VOICE_LIB_EXTS = (".wav", ".mp3", ".flac", ".ogg", ".m4a")
+
+_INVALID_NAME_CHARS = re.compile(r'[\\/:*?"<>|\x00-\x1f]')
+
+
+def safe_voice_name(name: str) -> str:
+    """Return a Windows-safe voice filename stem capped at 60 chars."""
+    cleaned = _INVALID_NAME_CHARS.sub("", (name or "").strip())
+    return cleaned[:60]
+
+
+def list_voices() -> list[dict]:
+    """List saved voices with name, relative path, extension, and byte size."""
+    library_dir = Path(VOICE_LIB_DIR)
+    if not library_dir.is_dir():
+        return []
+
+    items = []
+    for path in library_dir.iterdir():
+        if not path.is_file():
+            continue
+
+        ext = path.suffix.lower()
+        if ext not in VOICE_LIB_EXTS:
+            continue
+
+        try:
+            size_bytes = path.stat().st_size
+        except OSError:
+            continue
+
+        items.append(
+            {
+                "name": path.stem,
+                "path": _format_voice_path(path),
+                "ext": ext,
+                "size_bytes": size_bytes,
+            }
+        )
+
+    items.sort(key=lambda item: (item["name"].lower(), item["name"], item["ext"]))
+    return items
+
+
+def get_voice_path(name: str) -> Optional[str]:
+    """Return a saved voice path by name, or None when it does not exist."""
+    safe = safe_voice_name(name)
+    if not safe:
+        return None
+
+    path = _find_voice_path(safe)
+    if path is None:
+        return None
+    return _format_voice_path(path)
+
+
+def save_voice(audio_bytes: bytes, name: str, ext: str = ".wav") -> str:
+    """Save uploaded audio bytes and return the final voice file path."""
+    safe = safe_voice_name(name)
+    if not safe:
+        raise ValueError("voice name is empty")
+
+    normalized_ext = _normalize_voice_ext(ext)
+    dst = _voice_path(safe, normalized_ext)
+    dst.parent.mkdir(parents=True, exist_ok=True)
+    _delete_existing_voice_files(safe, keep=dst)
+
+    with dst.open("wb") as file:
+        file.write(audio_bytes)
+
+    return _format_voice_path(dst)
+
+
+def save_voice_from_path(source_path: str, name: str) -> str:
+    """Copy a local audio file into the voice library and return its path."""
+    safe = safe_voice_name(name)
+    if not safe:
+        raise ValueError("voice name is empty")
+
+    source = Path(source_path)
+    if not source.is_file():
+        raise FileNotFoundError(source_path)
+
+    normalized_ext = _normalize_voice_ext(source.suffix)
+    dst = _voice_path(safe, normalized_ext)
+    dst.parent.mkdir(parents=True, exist_ok=True)
+    _delete_existing_voice_files(safe, keep=dst)
+
+    if source.resolve(strict=False) != dst.resolve(strict=False):
+        shutil.copy2(source, dst)
+
+    return _format_voice_path(dst)
+
+
+def delete_voice(name: str) -> bool:
+    """Delete a saved voice by name, returning False when absent or failed."""
+    safe = safe_voice_name(name)
+    if not safe:
+        return False
+
+    path = _find_voice_path(safe)
+    if path is None:
+        return False
+
+    try:
+        path.unlink()
+    except OSError:
+        return False
+    return True
+
+
+def _normalize_voice_ext(ext: str) -> str:
+    ext = (ext or ".wav").lower()
+    if not ext.startswith("."):
+        ext = "." + ext
+    if ext not in VOICE_LIB_EXTS:
+        return ".wav"
+    return ext
+
+
+def _voice_path(name: str, ext: str) -> Path:
+    return Path(VOICE_LIB_DIR) / f"{name}{ext}"
+
+
+def _find_voice_path(safe_name: str) -> Optional[Path]:
+    library_dir = Path(VOICE_LIB_DIR)
+    for ext in VOICE_LIB_EXTS:
+        path = library_dir / f"{safe_name}{ext}"
+        if path.is_file():
+            return path
+
+    if not library_dir.is_dir():
+        return None
+
+    for ext in VOICE_LIB_EXTS:
+        target_name = f"{safe_name}{ext}".lower()
+        for path in library_dir.iterdir():
+            if path.is_file() and path.name.lower() == target_name:
+                return path
+
+    return None
+
+
+def _delete_existing_voice_files(safe_name: str, keep: Optional[Path] = None) -> None:
+    keep_resolved = keep.resolve(strict=False) if keep is not None else None
+    for ext in VOICE_LIB_EXTS:
+        path = _voice_path(safe_name, ext)
+        if keep_resolved is not None and path.resolve(strict=False) == keep_resolved:
+            continue
+        try:
+            path.unlink()
+        except FileNotFoundError:
+            continue
+
+
+def _format_voice_path(path: Path) -> str:
+    return path.as_posix()

codex
语法检查通过；`py_compile` 没有引入额外可见改动。接下来只 `git add` 目标文件，然后检查暂存内容再提交。
exec
"C:\\Program Files\\PowerShell\\7\\pwsh.exe" -Command 'git add indextts/voice_library.py' in D:\apiWorkSpace\index-tts2-vLLM
 succeeded in 549ms:
warning: in the working copy of 'indextts/voice_library.py', LF will be replaced by CRLF the next time Git touches it

diff --git a/indextts/voice_library.py b/indextts/voice_library.py
new file mode 100644
index 0000000000000000000000000000000000000000..4ee5d9b8f5f16a07feb288be39cfcdcdd438b696
--- /dev/null
+++ b/indextts/voice_library.py
@@ -0,0 +1,164 @@
+import re
+import shutil
+from pathlib import Path
+from typing import Optional
+
+
+VOICE_LIB_DIR = "prompts/library"
+VOICE_LIB_EXTS = (".wav", ".mp3", ".flac", ".ogg", ".m4a")
+
+_INVALID_NAME_CHARS = re.compile(r'[\\/:*?"<>|\x00-\x1f]')
+
+
+def safe_voice_name(name: str) -> str:
+    """Return a Windows-safe voice filename stem capped at 60 chars."""
+    cleaned = _INVALID_NAME_CHARS.sub("", (name or "").strip())
+    return cleaned[:60]
+
+
+def list_voices() -> list[dict]:
+    """List saved voices with name, relative path, extension, and byte size."""
+    library_dir = Path(VOICE_LIB_DIR)
+    if not library_dir.is_dir():
+        return []
+
+    items = []
+    for path in library_dir.iterdir():
+        if not path.is_file():
+            continue
+
+        ext = path.suffix.lower()
+        if ext not in VOICE_LIB_EXTS:
+            continue
+
+        try:
+            size_bytes = path.stat().st_size
+        except OSError:
+            continue
+
+        items.append(
+            {
+                "name": path.stem,
+                "path": _format_voice_path(path),
+                "ext": ext,
+                "size_bytes": size_bytes,
+            }
+        )
+
+    items.sort(key=lambda item: (item["name"].lower(), item["name"], item["ext"]))
+    return items
+
+
+def get_voice_path(name: str) -> Optional[str]:
+    """Return a saved voice path by name, or None when it does not exist."""
+    safe = safe_voice_name(name)
+    if not safe:
+        return None
+
+    path = _find_voice_path(safe)
+    if path is None:
+        return None
+    return _format_voice_path(path)
+
+
+def save_voice(audio_bytes: bytes, name: str, ext: str = ".wav") -> str:
+    """Save uploaded audio bytes and return the final voice file path."""
+    safe = safe_voice_name(name)
+    if not safe:
+        raise ValueError("voice name is empty")
+
+    normalized_ext = _normalize_voice_ext(ext)
+    dst = _voice_path(safe, normalized_ext)
+    dst.parent.mkdir(parents=True, exist_ok=True)
+    _delete_existing_voice_files(safe, keep=dst)
+
+    with dst.open("wb") as file:
+        file.write(audio_bytes)
+
+    return _format_voice_path(dst)
+
+
+def save_voice_from_path(source_path: str, name: str) -> str:
+    """Copy a local audio file into the voice library and return its path."""
+    safe = safe_voice_name(name)
+    if not safe:
+        raise ValueError("voice name is empty")
+
+    source = Path(source_path)
+    if not source.is_file():
+        raise FileNotFoundError(source_path)
+
+    normalized_ext = _normalize_voice_ext(source.suffix)
+    dst = _voice_path(safe, normalized_ext)
+    dst.parent.mkdir(parents=True, exist_ok=True)
+    _delete_existing_voice_files(safe, keep=dst)
+
+    if source.resolve(strict=False) != dst.resolve(strict=False):
+        shutil.copy2(source, dst)
+
+    return _format_voice_path(dst)
+
+
+def delete_voice(name: str) -> bool:
+    """Delete a saved voice by name, returning False when absent or failed."""
+    safe = safe_voice_name(name)
+    if not safe:
+        return False
+
+    path = _find_voice_path(safe)
+    if path is None:
+        return False
+
+    try:
+        path.unlink()
+    except OSError:
+        return False
+    return True
+
+
+def _normalize_voice_ext(ext: str) -> str:
+    ext = (ext or ".wav").lower()
+    if not ext.startswith("."):
+        ext = "." + ext
+    if ext not in VOICE_LIB_EXTS:
+        return ".wav"
+    return ext
+
+
+def _voice_path(name: str, ext: str) -> Path:
+    return Path(VOICE_LIB_DIR) / f"{name}{ext}"
+
+
+def _find_voice_path(safe_name: str) -> Optional[Path]:
+    library_dir = Path(VOICE_LIB_DIR)
+    for ext in VOICE_LIB_EXTS:
+        path = library_dir / f"{safe_name}{ext}"
+        if path.is_file():
+            return path
+
+    if not library_dir.is_dir():
+        return None
+
+    for ext in VOICE_LIB_EXTS:
+        target_name = f"{safe_name}{ext}".lower()
+        for path in library_dir.iterdir():
+            if path.is_file() and path.name.lower() == target_name:
+                return path
+
+    return None
+
+
+def _delete_existing_voice_files(safe_name: str, keep: Optional[Path] = None) -> None:
+    keep_resolved = keep.resolve(strict=False) if keep is not None else None
+    for ext in VOICE_LIB_EXTS:
+        path = _voice_path(safe_name, ext)
+        if keep_resolved is not None and path.resolve(strict=False) == keep_resolved:
+            continue
+        try:
+            path.unlink()
+        except FileNotFoundError:
+            continue
+
+
+def _format_voice_path(path: Path) -> str:
+    return path.as_posix()

exec
"C:\\Program Files\\PowerShell\\7\\pwsh.exe" -Command 'git diff --cached --stat' in D:\apiWorkSpace\index-tts2-vLLM
exec
"C:\\Program Files\\PowerShell\\7\\pwsh.exe" -Command 'git status --short' in D:\apiWorkSpace\index-tts2-vLLM
exec
"C:\\Program Files\\PowerShell\\7\\pwsh.exe" -Command 'git diff --cached -- indextts/voice_library.py' in D:\apiWorkSpace\index-tts2-vLLM
 succeeded in 457ms:
 indextts/voice_library.py | 164 ++++++++++++++++++++++++++++++++++++++++++++++
 1 file changed, 164 insertions(+)

 succeeded in 482ms:
A  indextts/voice_library.py
?? .claude/
?? .codex_dispatch_voice_library.log
?? "\351\237\263\350\211\262\345\217\202\350\200\203\351\237\263\351\242\221/"

 succeeded in 489ms:
diff --git a/indextts/voice_library.py b/indextts/voice_library.py
new file mode 100644
index 0000000..4ee5d9b
--- /dev/null
+++ b/indextts/voice_library.py
@@ -0,0 +1,164 @@
+import re
+import shutil
+from pathlib import Path
+from typing import Optional
+
+
+VOICE_LIB_DIR = "prompts/library"
+VOICE_LIB_EXTS = (".wav", ".mp3", ".flac", ".ogg", ".m4a")
+
+_INVALID_NAME_CHARS = re.compile(r'[\\/:*?"<>|\x00-\x1f]')
+
+
+def safe_voice_name(name: str) -> str:
+    """Return a Windows-safe voice filename stem capped at 60 chars."""
+    cleaned = _INVALID_NAME_CHARS.sub("", (name or "").strip())
+    return cleaned[:60]
+
+
+def list_voices() -> list[dict]:
+    """List saved voices with name, relative path, extension, and byte size."""
+    library_dir = Path(VOICE_LIB_DIR)
+    if not library_dir.is_dir():
+        return []
+
+    items = []
+    for path in library_dir.iterdir():
+        if not path.is_file():
+            continue
+
+        ext = path.suffix.lower()
+        if ext not in VOICE_LIB_EXTS:
+            continue
+
+        try:
+            size_bytes = path.stat().st_size
+        except OSError:
+            continue
+
+        items.append(
+            {
+                "name": path.stem,
+                "path": _format_voice_path(path),
+                "ext": ext,
+                "size_bytes": size_bytes,
+            }
+        )
+
+    items.sort(key=lambda item: (item["name"].lower(), item["name"], item["ext"]))
+    return items
+
+
+def get_voice_path(name: str) -> Optional[str]:
+    """Return a saved voice path by name, or None when it does not exist."""
+    safe = safe_voice_name(name)
+    if not safe:
+        return None
+
+    path = _find_voice_path(safe)
+    if path is None:
+        return None
+    return _format_voice_path(path)
+
+
+def save_voice(audio_bytes: bytes, name: str, ext: str = ".wav") -> str:
+    """Save uploaded audio bytes and return the final voice file path."""
+    safe = safe_voice_name(name)
+    if not safe:
+        raise ValueError("voice name is empty")
+
+    normalized_ext = _normalize_voice_ext(ext)
+    dst = _voice_path(safe, normalized_ext)
+    dst.parent.mkdir(parents=True, exist_ok=True)
+    _delete_existing_voice_files(safe, keep=dst)
+
+    with dst.open("wb") as file:
+        file.write(audio_bytes)
+
+    return _format_voice_path(dst)
+
+
+def save_voice_from_path(source_path: str, name: str) -> str:
+    """Copy a local audio file into the voice library and return its path."""
+    safe = safe_voice_name(name)
+    if not safe:
+        raise ValueError("voice name is empty")
+
+    source = Path(source_path)
+    if not source.is_file():
+        raise FileNotFoundError(source_path)
+
+    normalized_ext = _normalize_voice_ext(source.suffix)
+    dst = _voice_path(safe, normalized_ext)
+    dst.parent.mkdir(parents=True, exist_ok=True)
+    _delete_existing_voice_files(safe, keep=dst)
+
+    if source.resolve(strict=False) != dst.resolve(strict=False):
+        shutil.copy2(source, dst)
+
+    return _format_voice_path(dst)
+
+
+def delete_voice(name: str) -> bool:
+    """Delete a saved voice by name, returning False when absent or failed."""
+    safe = safe_voice_name(name)
+    if not safe:
+        return False
+
+    path = _find_voice_path(safe)
+    if path is None:
+        return False
+
+    try:
+        path.unlink()
+    except OSError:
+        return False
+    return True
+
+
+def _normalize_voice_ext(ext: str) -> str:
+    ext = (ext or ".wav").lower()
+    if not ext.startswith("."):
+        ext = "." + ext
+    if ext not in VOICE_LIB_EXTS:
+        return ".wav"
+    return ext
+
+
+def _voice_path(name: str, ext: str) -> Path:
+    return Path(VOICE_LIB_DIR) / f"{name}{ext}"
+
+
+def _find_voice_path(safe_name: str) -> Optional[Path]:
+    library_dir = Path(VOICE_LIB_DIR)
+    for ext in VOICE_LIB_EXTS:
+        path = library_dir / f"{safe_name}{ext}"
+        if path.is_file():
+            return path
+
+    if not library_dir.is_dir():
+        return None
+
+    for ext in VOICE_LIB_EXTS:
+        target_name = f"{safe_name}{ext}".lower()
+        for path in library_dir.iterdir():
+            if path.is_file() and path.name.lower() == target_name:
+                return path
+
+    return None
+
+
+def _delete_existing_voice_files(safe_name: str, keep: Optional[Path] = None) -> None:
+    keep_resolved = keep.resolve(strict=False) if keep is not None else None
+    for ext in VOICE_LIB_EXTS:
+        path = _voice_path(safe_name, ext)
+        if keep_resolved is not None and path.resolve(strict=False) == keep_resolved:
+            continue
+        try:
+            path.unlink()
+        except FileNotFoundError:
+            continue
+
+
+def _format_voice_path(path: Path) -> str:
+    return path.as_posix()

diff --git a/indextts/voice_library.py b/indextts/voice_library.py
new file mode 100644
index 0000000000000000000000000000000000000000..4ee5d9b8f5f16a07feb288be39cfcdcdd438b696
--- /dev/null
+++ b/indextts/voice_library.py
@@ -0,0 +1,164 @@
+import re
+import shutil
+from pathlib import Path
+from typing import Optional
+
+
+VOICE_LIB_DIR = "prompts/library"
+VOICE_LIB_EXTS = (".wav", ".mp3", ".flac", ".ogg", ".m4a")
+
+_INVALID_NAME_CHARS = re.compile(r'[\\/:*?"<>|\x00-\x1f]')
+
+
+def safe_voice_name(name: str) -> str:
+    """Return a Windows-safe voice filename stem capped at 60 chars."""
+    cleaned = _INVALID_NAME_CHARS.sub("", (name or "").strip())
+    return cleaned[:60]
+
+
+def list_voices() -> list[dict]:
+    """List saved voices with name, relative path, extension, and byte size."""
+    library_dir = Path(VOICE_LIB_DIR)
+    if not library_dir.is_dir():
+        return []
+
+    items = []
+    for path in library_dir.iterdir():
+        if not path.is_file():
+            continue
+
+        ext = path.suffix.lower()
+        if ext not in VOICE_LIB_EXTS:
+            continue
+
+        try:
+            size_bytes = path.stat().st_size
+        except OSError:
+            continue
+
+        items.append(
+            {
+                "name": path.stem,
+                "path": _format_voice_path(path),
+                "ext": ext,
+                "size_bytes": size_bytes,
+            }
+        )
+
+    items.sort(key=lambda item: (item["name"].lower(), item["name"], item["ext"]))
+    return items
+
+
+def get_voice_path(name: str) -> Optional[str]:
+    """Return a saved voice path by name, or None when it does not exist."""
+    safe = safe_voice_name(name)
+    if not safe:
+        return None
+
+    path = _find_voice_path(safe)
+    if path is None:
+        return None
+    return _format_voice_path(path)
+
+
+def save_voice(audio_bytes: bytes, name: str, ext: str = ".wav") -> str:
+    """Save uploaded audio bytes and return the final voice file path."""
+    safe = safe_voice_name(name)
+    if not safe:
+        raise ValueError("voice name is empty")
+
+    normalized_ext = _normalize_voice_ext(ext)
+    dst = _voice_path(safe, normalized_ext)
+    dst.parent.mkdir(parents=True, exist_ok=True)
+    _delete_existing_voice_files(safe, keep=dst)
+
+    with dst.open("wb") as file:
+        file.write(audio_bytes)
+
+    return _format_voice_path(dst)
+
+
+def save_voice_from_path(source_path: str, name: str) -> str:
+    """Copy a local audio file into the voice library and return its path."""
+    safe = safe_voice_name(name)
+    if not safe:
+        raise ValueError("voice name is empty")
+
+    source = Path(source_path)
+    if not source.is_file():
+        raise FileNotFoundError(source_path)
+
+    normalized_ext = _normalize_voice_ext(source.suffix)
+    dst = _voice_path(safe, normalized_ext)
+    dst.parent.mkdir(parents=True, exist_ok=True)
+    _delete_existing_voice_files(safe, keep=dst)
+
+    if source.resolve(strict=False) != dst.resolve(strict=False):
+        shutil.copy2(source, dst)
+
+    return _format_voice_path(dst)
+
+
+def delete_voice(name: str) -> bool:
+    """Delete a saved voice by name, returning False when absent or failed."""
+    safe = safe_voice_name(name)
+    if not safe:
+        return False
+
+    path = _find_voice_path(safe)
+    if path is None:
+        return False
+
+    try:
+        path.unlink()
+    except OSError:
+        return False
+    return True
+
+
+def _normalize_voice_ext(ext: str) -> str:
+    ext = (ext or ".wav").lower()
+    if not ext.startswith("."):
+        ext = "." + ext
+    if ext not in VOICE_LIB_EXTS:
+        return ".wav"
+    return ext
+
+
+def _voice_path(name: str, ext: str) -> Path:
+    return Path(VOICE_LIB_DIR) / f"{name}{ext}"
+
+
+def _find_voice_path(safe_name: str) -> Optional[Path]:
+    library_dir = Path(VOICE_LIB_DIR)
+    for ext in VOICE_LIB_EXTS:
+        path = library_dir / f"{safe_name}{ext}"
+        if path.is_file():
+            return path
+
+    if not library_dir.is_dir():
+        return None
+
+    for ext in VOICE_LIB_EXTS:
+        target_name = f"{safe_name}{ext}".lower()
+        for path in library_dir.iterdir():
+            if path.is_file() and path.name.lower() == target_name:
+                return path
+
+    return None
+
+
+def _delete_existing_voice_files(safe_name: str, keep: Optional[Path] = None) -> None:
+    keep_resolved = keep.resolve(strict=False) if keep is not None else None
+    for ext in VOICE_LIB_EXTS:
+        path = _voice_path(safe_name, ext)
+        if keep_resolved is not None and path.resolve(strict=False) == keep_resolved:
+            continue
+        try:
+            path.unlink()
+        except FileNotFoundError:
+            continue
+
+
+def _format_voice_path(path: Path) -> str:
+    return path.as_posix()

codex
暂存区只有 `indextts/voice_library.py`。我现在按指定提交信息提交，随后推到 `userrepo VLLM`。
exec
"C:\\Program Files\\PowerShell\\7\\pwsh.exe" -Command 'git commit -m "[codex] Add voice_library module for Phase 2/3 voice CRUD"' in D:\apiWorkSpace\index-tts2-vLLM
 succeeded in 811ms:
[VLLM 353febc] [codex] Add voice_library module for Phase 2/3 voice CRUD
 1 file changed, 164 insertions(+)
 create mode 100644 indextts/voice_library.py

diff --git a/indextts/voice_library.py b/indextts/voice_library.py
new file mode 100644
index 0000000000000000000000000000000000000000..4ee5d9b8f5f16a07feb288be39cfcdcdd438b696
--- /dev/null
+++ b/indextts/voice_library.py
@@ -0,0 +1,164 @@
+import re
+import shutil
+from pathlib import Path
+from typing import Optional
+
+
+VOICE_LIB_DIR = "prompts/library"
+VOICE_LIB_EXTS = (".wav", ".mp3", ".flac", ".ogg", ".m4a")
+
+_INVALID_NAME_CHARS = re.compile(r'[\\/:*?"<>|\x00-\x1f]')
+
+
+def safe_voice_name(name: str) -> str:
+    """Return a Windows-safe voice filename stem capped at 60 chars."""
+    cleaned = _INVALID_NAME_CHARS.sub("", (name or "").strip())
+    return cleaned[:60]
+
+
+def list_voices() -> list[dict]:
+    """List saved voices with name, relative path, extension, and byte size."""
+    library_dir = Path(VOICE_LIB_DIR)
+    if not library_dir.is_dir():
+        return []
+
+    items = []
+    for path in library_dir.iterdir():
+        if not path.is_file():
+            continue
+
+        ext = path.suffix.lower()
+        if ext not in VOICE_LIB_EXTS:
+            continue
+
+        try:
+            size_bytes = path.stat().st_size
+        except OSError:
+            continue
+
+        items.append(
+            {
+                "name": path.stem,
+                "path": _format_voice_path(path),
+                "ext": ext,
+                "size_bytes": size_bytes,
+            }
+        )
+
+    items.sort(key=lambda item: (item["name"].lower(), item["name"], item["ext"]))
+    return items
+
+
+def get_voice_path(name: str) -> Optional[str]:
+    """Return a saved voice path by name, or None when it does not exist."""
+    safe = safe_voice_name(name)
+    if not safe:
+        return None
+
+    path = _find_voice_path(safe)
+    if path is None:
+        return None
+    return _format_voice_path(path)
+
+
+def save_voice(audio_bytes: bytes, name: str, ext: str = ".wav") -> str:
+    """Save uploaded audio bytes and return the final voice file path."""
+    safe = safe_voice_name(name)
+    if not safe:
+        raise ValueError("voice name is empty")
+
+    normalized_ext = _normalize_voice_ext(ext)
+    dst = _voice_path(safe, normalized_ext)
+    dst.parent.mkdir(parents=True, exist_ok=True)
+    _delete_existing_voice_files(safe, keep=dst)
+
+    with dst.open("wb") as file:
+        file.write(audio_bytes)
+
+    return _format_voice_path(dst)
+
+
+def save_voice_from_path(source_path: str, name: str) -> str:
+    """Copy a local audio file into the voice library and return its path."""
+    safe = safe_voice_name(name)
+    if not safe:
+        raise ValueError("voice name is empty")
+
+    source = Path(source_path)
+    if not source.is_file():
+        raise FileNotFoundError(source_path)
+
+    normalized_ext = _normalize_voice_ext(source.suffix)
+    dst = _voice_path(safe, normalized_ext)
+    dst.parent.mkdir(parents=True, exist_ok=True)
+    _delete_existing_voice_files(safe, keep=dst)
+
+    if source.resolve(strict=False) != dst.resolve(strict=False):
+        shutil.copy2(source, dst)
+
+    return _format_voice_path(dst)
+
+
+def delete_voice(name: str) -> bool:
+    """Delete a saved voice by name, returning False when absent or failed."""
+    safe = safe_voice_name(name)
+    if not safe:
+        return False
+
+    path = _find_voice_path(safe)
+    if path is None:
+        return False
+
+    try:
+        path.unlink()
+    except OSError:
+        return False
+    return True
+
+
+def _normalize_voice_ext(ext: str) -> str:
+    ext = (ext or ".wav").lower()
+    if not ext.startswith("."):
+        ext = "." + ext
+    if ext not in VOICE_LIB_EXTS:
+        return ".wav"
+    return ext
+
+
+def _voice_path(name: str, ext: str) -> Path:
+    return Path(VOICE_LIB_DIR) / f"{name}{ext}"
+
+
+def _find_voice_path(safe_name: str) -> Optional[Path]:
+    library_dir = Path(VOICE_LIB_DIR)
+    for ext in VOICE_LIB_EXTS:
+        path = library_dir / f"{safe_name}{ext}"
+        if path.is_file():
+            return path
+
+    if not library_dir.is_dir():
+        return None
+
+    for ext in VOICE_LIB_EXTS:
+        target_name = f"{safe_name}{ext}".lower()
+        for path in library_dir.iterdir():
+            if path.is_file() and path.name.lower() == target_name:
+                return path
+
+    return None
+
+
+def _delete_existing_voice_files(safe_name: str, keep: Optional[Path] = None) -> None:
+    keep_resolved = keep.resolve(strict=False) if keep is not None else None
+    for ext in VOICE_LIB_EXTS:
+        path = _voice_path(safe_name, ext)
+        if keep_resolved is not None and path.resolve(strict=False) == keep_resolved:
+            continue
+        try:
+            path.unlink()
+        except FileNotFoundError:
+            continue
+
+
+def _format_voice_path(path: Path) -> str:
+    return path.as_posix()

exec
"C:\\Program Files\\PowerShell\\7\\pwsh.exe" -Command 'git push userrepo VLLM' in D:\apiWorkSpace\index-tts2-vLLM
2026-05-24T17:50:23.958936Z ERROR codex_core::tools::router: error=Exit code: 1
Wall time: 0.5 seconds
Output:
fatal: unable to access 'https://github.com/928886540/index-tts-vLLM.git/': schannel: AcquireCredentialsHandle failed: SEC_E_NO_CREDENTIALS (0x8009030e)

 exited 1 in 466ms:
fatal: unable to access 'https://github.com/928886540/index-tts-vLLM.git/': schannel: AcquireCredentialsHandle failed: SEC_E_NO_CREDENTIALS (0x8009030e)

diff --git a/indextts/voice_library.py b/indextts/voice_library.py
new file mode 100644
index 0000000000000000000000000000000000000000..4ee5d9b8f5f16a07feb288be39cfcdcdd438b696
--- /dev/null
+++ b/indextts/voice_library.py
@@ -0,0 +1,164 @@
+import re
+import shutil
+from pathlib import Path
+from typing import Optional
+
+
+VOICE_LIB_DIR = "prompts/library"
+VOICE_LIB_EXTS = (".wav", ".mp3", ".flac", ".ogg", ".m4a")
+
+_INVALID_NAME_CHARS = re.compile(r'[\\/:*?"<>|\x00-\x1f]')
+
+
+def safe_voice_name(name: str) -> str:
+    """Return a Windows-safe voice filename stem capped at 60 chars."""
+    cleaned = _INVALID_NAME_CHARS.sub("", (name or "").strip())
+    return cleaned[:60]
+
+
+def list_voices() -> list[dict]:
+    """List saved voices with name, relative path, extension, and byte size."""
+    library_dir = Path(VOICE_LIB_DIR)
+    if not library_dir.is_dir():
+        return []
+
+    items = []
+    for path in library_dir.iterdir():
+        if not path.is_file():
+            continue
+
+        ext = path.suffix.lower()
+        if ext not in VOICE_LIB_EXTS:
+            continue
+
+        try:
+            size_bytes = path.stat().st_size
+        except OSError:
+            continue
+
+        items.append(
+            {
+                "name": path.stem,
+                "path": _format_voice_path(path),
+                "ext": ext,
+                "size_bytes": size_bytes,
+            }
+        )
+
+    items.sort(key=lambda item: (item["name"].lower(), item["name"], item["ext"]))
+    return items
+
+
+def get_voice_path(name: str) -> Optional[str]:
+    """Return a saved voice path by name, or None when it does not exist."""
+    safe = safe_voice_name(name)
+    if not safe:
+        return None
+
+    path = _find_voice_path(safe)
+    if path is None:
+        return None
+    return _format_voice_path(path)
+
+
+def save_voice(audio_bytes: bytes, name: str, ext: str = ".wav") -> str:
+    """Save uploaded audio bytes and return the final voice file path."""
+    safe = safe_voice_name(name)
+    if not safe:
+        raise ValueError("voice name is empty")
+
+    normalized_ext = _normalize_voice_ext(ext)
+    dst = _voice_path(safe, normalized_ext)
+    dst.parent.mkdir(parents=True, exist_ok=True)
+    _delete_existing_voice_files(safe, keep=dst)
+
+    with dst.open("wb") as file:
+        file.write(audio_bytes)
+
+    return _format_voice_path(dst)
+
+
+def save_voice_from_path(source_path: str, name: str) -> str:
+    """Copy a local audio file into the voice library and return its path."""
+    safe = safe_voice_name(name)
+    if not safe:
+        raise ValueError("voice name is empty")
+
+    source = Path(source_path)
+    if not source.is_file():
+        raise FileNotFoundError(source_path)
+
+    normalized_ext = _normalize_voice_ext(source.suffix)
+    dst = _voice_path(safe, normalized_ext)
+    dst.parent.mkdir(parents=True, exist_ok=True)
+    _delete_existing_voice_files(safe, keep=dst)
+
+    if source.resolve(strict=False) != dst.resolve(strict=False):
+        shutil.copy2(source, dst)
+
+    return _format_voice_path(dst)
+
+
+def delete_voice(name: str) -> bool:
+    """Delete a saved voice by name, returning False when absent or failed."""
+    safe = safe_voice_name(name)
+    if not safe:
+        return False
+
+    path = _find_voice_path(safe)
+    if path is None:
+        return False
+
+    try:
+        path.unlink()
+    except OSError:
+        return False
+    return True
+
+
+def _normalize_voice_ext(ext: str) -> str:
+    ext = (ext or ".wav").lower()
+    if not ext.startswith("."):
+        ext = "." + ext
+    if ext not in VOICE_LIB_EXTS:
+        return ".wav"
+    return ext
+
+
+def _voice_path(name: str, ext: str) -> Path:
+    return Path(VOICE_LIB_DIR) / f"{name}{ext}"
+
+
+def _find_voice_path(safe_name: str) -> Optional[Path]:
+    library_dir = Path(VOICE_LIB_DIR)
+    for ext in VOICE_LIB_EXTS:
+        path = library_dir / f"{safe_name}{ext}"
+        if path.is_file():
+            return path
+
+    if not library_dir.is_dir():
+        return None
+
+    for ext in VOICE_LIB_EXTS:
+        target_name = f"{safe_name}{ext}".lower()
+        for path in library_dir.iterdir():
+            if path.is_file() and path.name.lower() == target_name:
+                return path
+
+    return None
+
+
+def _delete_existing_voice_files(safe_name: str, keep: Optional[Path] = None) -> None:
+    keep_resolved = keep.resolve(strict=False) if keep is not None else None
+    for ext in VOICE_LIB_EXTS:
+        path = _voice_path(safe_name, ext)
+        if keep_resolved is not None and path.resolve(strict=False) == keep_resolved:
+            continue
+        try:
+            path.unlink()
+        except FileNotFoundError:
+            continue
+
+
+def _format_voice_path(path: Path) -> str:
+    return path.as_posix()

codex
提交已生成 `353febc`。推送时远端需要凭据，当前环境没有可用凭据；我先确认远端配置，避免误推到错误地址。
exec
"C:\\Program Files\\PowerShell\\7\\pwsh.exe" -Command 'git remote -v' in D:\apiWorkSpace\index-tts2-vLLM
exec
"C:\\Program Files\\PowerShell\\7\\pwsh.exe" -Command 'git status --short' in D:\apiWorkSpace\index-tts2-vLLM
exec
"C:\\Program Files\\PowerShell\\7\\pwsh.exe" -Command 'git rev-parse --short HEAD' in D:\apiWorkSpace\index-tts2-vLLM
 succeeded in 508ms:
origin	https://github.com/928886540/index-tts-vLLM (fetch)
origin	https://github.com/928886540/index-tts-vLLM (push)
other	https://github.com/Ksuriuri/index-tts-vllm.git (fetch)
other	https://github.com/Ksuriuri/index-tts-vllm.git (push)
userrepo	https://github.com/928886540/index-tts-vLLM.git (fetch)
userrepo	https://github.com/928886540/index-tts-vLLM.git (push)

 succeeded in 529ms:
?? .claude/
?? .codex_dispatch_voice_library.log
?? "\351\237\263\350\211\262\345\217\202\350\200\203\351\237\263\351\242\221/"

 succeeded in 470ms:
353febc

diff --git a/indextts/voice_library.py b/indextts/voice_library.py
new file mode 100644
index 0000000000000000000000000000000000000000..4ee5d9b8f5f16a07feb288be39cfcdcdd438b696
--- /dev/null
+++ b/indextts/voice_library.py
@@ -0,0 +1,164 @@
+import re
+import shutil
+from pathlib import Path
+from typing import Optional
+
+
+VOICE_LIB_DIR = "prompts/library"
+VOICE_LIB_EXTS = (".wav", ".mp3", ".flac", ".ogg", ".m4a")
+
+_INVALID_NAME_CHARS = re.compile(r'[\\/:*?"<>|\x00-\x1f]')
+
+
+def safe_voice_name(name: str) -> str:
+    """Return a Windows-safe voice filename stem capped at 60 chars."""
+    cleaned = _INVALID_NAME_CHARS.sub("", (name or "").strip())
+    return cleaned[:60]
+
+
+def list_voices() -> list[dict]:
+    """List saved voices with name, relative path, extension, and byte size."""
+    library_dir = Path(VOICE_LIB_DIR)
+    if not library_dir.is_dir():
+        return []
+
+    items = []
+    for path in library_dir.iterdir():
+        if not path.is_file():
+            continue
+
+        ext = path.suffix.lower()
+        if ext not in VOICE_LIB_EXTS:
+            continue
+
+        try:
+            size_bytes = path.stat().st_size
+        except OSError:
+            continue
+
+        items.append(
+            {
+                "name": path.stem,
+                "path": _format_voice_path(path),
+                "ext": ext,
+                "size_bytes": size_bytes,
+            }
+        )
+
+    items.sort(key=lambda item: (item["name"].lower(), item["name"], item["ext"]))
+    return items
+
+
+def get_voice_path(name: str) -> Optional[str]:
+    """Return a saved voice path by name, or None when it does not exist."""
+    safe = safe_voice_name(name)
+    if not safe:
+        return None
+
+    path = _find_voice_path(safe)
+    if path is None:
+        return None
+    return _format_voice_path(path)
+
+
+def save_voice(audio_bytes: bytes, name: str, ext: str = ".wav") -> str:
+    """Save uploaded audio bytes and return the final voice file path."""
+    safe = safe_voice_name(name)
+    if not safe:
+        raise ValueError("voice name is empty")
+
+    normalized_ext = _normalize_voice_ext(ext)
+    dst = _voice_path(safe, normalized_ext)
+    dst.parent.mkdir(parents=True, exist_ok=True)
+    _delete_existing_voice_files(safe, keep=dst)
+
+    with dst.open("wb") as file:
+        file.write(audio_bytes)
+
+    return _format_voice_path(dst)
+
+
+def save_voice_from_path(source_path: str, name: str) -> str:
+    """Copy a local audio file into the voice library and return its path."""
+    safe = safe_voice_name(name)
+    if not safe:
+        raise ValueError("voice name is empty")
+
+    source = Path(source_path)
+    if not source.is_file():
+        raise FileNotFoundError(source_path)
+
+    normalized_ext = _normalize_voice_ext(source.suffix)
+    dst = _voice_path(safe, normalized_ext)
+    dst.parent.mkdir(parents=True, exist_ok=True)
+    _delete_existing_voice_files(safe, keep=dst)
+
+    if source.resolve(strict=False) != dst.resolve(strict=False):
+        shutil.copy2(source, dst)
+
+    return _format_voice_path(dst)
+
+
+def delete_voice(name: str) -> bool:
+    """Delete a saved voice by name, returning False when absent or failed."""
+    safe = safe_voice_name(name)
+    if not safe:
+        return False
+
+    path = _find_voice_path(safe)
+    if path is None:
+        return False
+
+    try:
+        path.unlink()
+    except OSError:
+        return False
+    return True
+
+
+def _normalize_voice_ext(ext: str) -> str:
+    ext = (ext or ".wav").lower()
+    if not ext.startswith("."):
+        ext = "." + ext
+    if ext not in VOICE_LIB_EXTS:
+        return ".wav"
+    return ext
+
+
+def _voice_path(name: str, ext: str) -> Path:
+    return Path(VOICE_LIB_DIR) / f"{name}{ext}"
+
+
+def _find_voice_path(safe_name: str) -> Optional[Path]:
+    library_dir = Path(VOICE_LIB_DIR)
+    for ext in VOICE_LIB_EXTS:
+        path = library_dir / f"{safe_name}{ext}"
+        if path.is_file():
+            return path
+
+    if not library_dir.is_dir():
+        return None
+
+    for ext in VOICE_LIB_EXTS:
+        target_name = f"{safe_name}{ext}".lower()
+        for path in library_dir.iterdir():
+            if path.is_file() and path.name.lower() == target_name:
+                return path
+
+    return None
+
+
+def _delete_existing_voice_files(safe_name: str, keep: Optional[Path] = None) -> None:
+    keep_resolved = keep.resolve(strict=False) if keep is not None else None
+    for ext in VOICE_LIB_EXTS:
+        path = _voice_path(safe_name, ext)
+        if keep_resolved is not None and path.resolve(strict=False) == keep_resolved:
+            continue
+        try:
+            path.unlink()
+        except FileNotFoundError:
+            continue
+
+
+def _format_voice_path(path: Path) -> str:
+    return path.as_posix()

codex
`userrepo` 确实指向用户仓库 HTTPS。这个报错来自 Git for Windows 的 Schannel TLS 层，我会用命令级 `http.sslBackend=openssl` 再试一次，不改全局配置。
exec
"C:\\Program Files\\PowerShell\\7\\pwsh.exe" -Command 'git -c http.sslBackend=openssl push userrepo VLLM' in D:\apiWorkSpace\index-tts2-vLLM
2026-05-24T17:52:47.614570Z ERROR codex_core::tools::router: error=Exit code: 124
Wall time: 120.2 seconds
Output:
command timed out after 120177 milliseconds

 exited 124 in 120177ms:
diff --git a/indextts/voice_library.py b/indextts/voice_library.py
new file mode 100644
index 0000000000000000000000000000000000000000..4ee5d9b8f5f16a07feb288be39cfcdcdd438b696
--- /dev/null
+++ b/indextts/voice_library.py
@@ -0,0 +1,164 @@
+import re
+import shutil
+from pathlib import Path
+from typing import Optional
+
+
+VOICE_LIB_DIR = "prompts/library"
+VOICE_LIB_EXTS = (".wav", ".mp3", ".flac", ".ogg", ".m4a")
+
+_INVALID_NAME_CHARS = re.compile(r'[\\/:*?"<>|\x00-\x1f]')
+
+
+def safe_voice_name(name: str) -> str:
+    """Return a Windows-safe voice filename stem capped at 60 chars."""
+    cleaned = _INVALID_NAME_CHARS.sub("", (name or "").strip())
+    return cleaned[:60]
+
+
+def list_voices() -> list[dict]:
+    """List saved voices with name, relative path, extension, and byte size."""
+    library_dir = Path(VOICE_LIB_DIR)
+    if not library_dir.is_dir():
+        return []
+
+    items = []
+    for path in library_dir.iterdir():
+        if not path.is_file():
+            continue
+
+        ext = path.suffix.lower()
+        if ext not in VOICE_LIB_EXTS:
+            continue
+
+        try:
+            size_bytes = path.stat().st_size
+        except OSError:
+            continue
+
+        items.append(
+            {
+                "name": path.stem,
+                "path": _format_voice_path(path),
+                "ext": ext,
+                "size_bytes": size_bytes,
+            }
+        )
+
+    items.sort(key=lambda item: (item["name"].lower(), item["name"], item["ext"]))
+    return items
+
+
+def get_voice_path(name: str) -> Optional[str]:
+    """Return a saved voice path by name, or None when it does not exist."""
+    safe = safe_voice_name(name)
+    if not safe:
+        return None
+
+    path = _find_voice_path(safe)
+    if path is None:
+        return None
+    return _format_voice_path(path)
+
+
+def save_voice(audio_bytes: bytes, name: str, ext: str = ".wav") -> str:
+    """Save uploaded audio bytes and return the final voice file path."""
+    safe = safe_voice_name(name)
+    if not safe:
+        raise ValueError("voice name is empty")
+
+    normalized_ext = _normalize_voice_ext(ext)
+    dst = _voice_path(safe, normalized_ext)
+    dst.parent.mkdir(parents=True, exist_ok=True)
+    _delete_existing_voice_files(safe, keep=dst)
+
+    with dst.open("wb") as file:
+        file.write(audio_bytes)
+
+    return _format_voice_path(dst)
+
+
+def save_voice_from_path(source_path: str, name: str) -> str:
+    """Copy a local audio file into the voice library and return its path."""
+    safe = safe_voice_name(name)
+    if not safe:
+        raise ValueError("voice name is empty")
+
+    source = Path(source_path)
+    if not source.is_file():
+        raise FileNotFoundError(source_path)
+
+    normalized_ext = _normalize_voice_ext(source.suffix)
+    dst = _voice_path(safe, normalized_ext)
+    dst.parent.mkdir(parents=True, exist_ok=True)
+    _delete_existing_voice_files(safe, keep=dst)
+
+    if source.resolve(strict=False) != dst.resolve(strict=False):
+        shutil.copy2(source, dst)
+
+    return _format_voice_path(dst)
+
+
+def delete_voice(name: str) -> bool:
+    """Delete a saved voice by name, returning False when absent or failed."""
+    safe = safe_voice_name(name)
+    if not safe:
+        return False
+
+    path = _find_voice_path(safe)
+    if path is None:
+        return False
+
+    try:
+        path.unlink()
+    except OSError:
+        return False
+    return True
+
+
+def _normalize_voice_ext(ext: str) -> str:
+    ext = (ext or ".wav").lower()
+    if not ext.startswith("."):
+        ext = "." + ext
+    if ext not in VOICE_LIB_EXTS:
+        return ".wav"
+    return ext
+
+
+def _voice_path(name: str, ext: str) -> Path:
+    return Path(VOICE_LIB_DIR) / f"{name}{ext}"
+
+
+def _find_voice_path(safe_name: str) -> Optional[Path]:
+    library_dir = Path(VOICE_LIB_DIR)
+    for ext in VOICE_LIB_EXTS:
+        path = library_dir / f"{safe_name}{ext}"
+        if path.is_file():
+            return path
+
+    if not library_dir.is_dir():
+        return None
+
+    for ext in VOICE_LIB_EXTS:
+        target_name = f"{safe_name}{ext}".lower()
+        for path in library_dir.iterdir():
+            if path.is_file() and path.name.lower() == target_name:
+                return path
+
+    return None
+
+
+def _delete_existing_voice_files(safe_name: str, keep: Optional[Path] = None) -> None:
+    keep_resolved = keep.resolve(strict=False) if keep is not None else None
+    for ext in VOICE_LIB_EXTS:
+        path = _voice_path(safe_name, ext)
+        if keep_resolved is not None and path.resolve(strict=False) == keep_resolved:
+            continue
+        try:
+            path.unlink()
+        except FileNotFoundError:
+            continue
+
+
+def _format_voice_path(path: Path) -> str:
+    return path.as_posix()

codex
第二次推送没有 Schannel 错误，但 120 秒超时，可能卡在 HTTPS 认证/网络等待。我检查一下是否还有残留 Git 进程，再确认本地状态。
exec
"C:\\Program Files\\PowerShell\\7\\pwsh.exe" -Command 'Get-Process git -ErrorAction SilentlyContinue | Select-Object Id,ProcessName,StartTime' in D:\apiWorkSpace\index-tts2-vLLM
exec
"C:\\Program Files\\PowerShell\\7\\pwsh.exe" -Command 'git status --short --branch' in D:\apiWorkSpace\index-tts2-vLLM
exec
"C:\\Program Files\\PowerShell\\7\\pwsh.exe" -Command 'git log -1 --oneline' in D:\apiWorkSpace\index-tts2-vLLM
 succeeded in 511ms:
## VLLM...origin/VLLM [ahead 1]
?? .claude/
?? .codex_dispatch_voice_library.log
?? "\351\237\263\350\211\262\345\217\202\350\200\203\351\237\263\351\242\221/"

 succeeded in 516ms:

[32;1m   Id[0m[32;1m ProcessName[0m[32;1m StartTime[0m
[32;1m   --[0m [32;1m-----------[0m [32;1m---------[0m
22728 git         2026/5/25 1:52:55


 succeeded in 460ms:
353febc [codex] Add voice_library module for Phase 2/3 voice CRUD

diff --git a/indextts/voice_library.py b/indextts/voice_library.py
new file mode 100644
index 0000000000000000000000000000000000000000..4ee5d9b8f5f16a07feb288be39cfcdcdd438b696
--- /dev/null
+++ b/indextts/voice_library.py
@@ -0,0 +1,164 @@
+import re
+import shutil
+from pathlib import Path
+from typing import Optional
+
+
+VOICE_LIB_DIR = "prompts/library"
+VOICE_LIB_EXTS = (".wav", ".mp3", ".flac", ".ogg", ".m4a")
+
+_INVALID_NAME_CHARS = re.compile(r'[\\/:*?"<>|\x00-\x1f]')
+
+
+def safe_voice_name(name: str) -> str:
+    """Return a Windows-safe voice filename stem capped at 60 chars."""
+    cleaned = _INVALID_NAME_CHARS.sub("", (name or "").strip())
+    return cleaned[:60]
+
+
+def list_voices() -> list[dict]:
+    """List saved voices with name, relative path, extension, and byte size."""
+    library_dir = Path(VOICE_LIB_DIR)
+    if not library_dir.is_dir():
+        return []
+
+    items = []
+    for path in library_dir.iterdir():
+        if not path.is_file():
+            continue
+
+        ext = path.suffix.lower()
+        if ext not in VOICE_LIB_EXTS:
+            continue
+
+        try:
+            size_bytes = path.stat().st_size
+        except OSError:
+            continue
+
+        items.append(
+            {
+                "name": path.stem,
+                "path": _format_voice_path(path),
+                "ext": ext,
+                "size_bytes": size_bytes,
+            }
+        )
+
+    items.sort(key=lambda item: (item["name"].lower(), item["name"], item["ext"]))
+    return items
+
+
+def get_voice_path(name: str) -> Optional[str]:
+    """Return a saved voice path by name, or None when it does not exist."""
+    safe = safe_voice_name(name)
+    if not safe:
+        return None
+
+    path = _find_voice_path(safe)
+    if path is None:
+        return None
+    return _format_voice_path(path)
+
+
+def save_voice(audio_bytes: bytes, name: str, ext: str = ".wav") -> str:
+    """Save uploaded audio bytes and return the final voice file path."""
+    safe = safe_voice_name(name)
+    if not safe:
+        raise ValueError("voice name is empty")
+
+    normalized_ext = _normalize_voice_ext(ext)
+    dst = _voice_path(safe, normalized_ext)
+    dst.parent.mkdir(parents=True, exist_ok=True)
+    _delete_existing_voice_files(safe, keep=dst)
+
+    with dst.open("wb") as file:
+        file.write(audio_bytes)
+
+    return _format_voice_path(dst)
+
+
+def save_voice_from_path(source_path: str, name: str) -> str:
+    """Copy a local audio file into the voice library and return its path."""
+    safe = safe_voice_name(name)
+    if not safe:
+        raise ValueError("voice name is empty")
+
+    source = Path(source_path)
+    if not source.is_file():
+        raise FileNotFoundError(source_path)
+
+    normalized_ext = _normalize_voice_ext(source.suffix)
+    dst = _voice_path(safe, normalized_ext)
+    dst.parent.mkdir(parents=True, exist_ok=True)
+    _delete_existing_voice_files(safe, keep=dst)
+
+    if source.resolve(strict=False) != dst.resolve(strict=False):
+        shutil.copy2(source, dst)
+
+    return _format_voice_path(dst)
+
+
+def delete_voice(name: str) -> bool:
+    """Delete a saved voice by name, returning False when absent or failed."""
+    safe = safe_voice_name(name)
+    if not safe:
+        return False
+
+    path = _find_voice_path(safe)
+    if path is None:
+        return False
+
+    try:
+        path.unlink()
+    except OSError:
+        return False
+    return True
+
+
+def _normalize_voice_ext(ext: str) -> str:
+    ext = (ext or ".wav").lower()
+    if not ext.startswith("."):
+        ext = "." + ext
+    if ext not in VOICE_LIB_EXTS:
+        return ".wav"
+    return ext
+
+
+def _voice_path(name: str, ext: str) -> Path:
+    return Path(VOICE_LIB_DIR) / f"{name}{ext}"
+
+
+def _find_voice_path(safe_name: str) -> Optional[Path]:
+    library_dir = Path(VOICE_LIB_DIR)
+    for ext in VOICE_LIB_EXTS:
+        path = library_dir / f"{safe_name}{ext}"
+        if path.is_file():
+            return path
+
+    if not library_dir.is_dir():
+        return None
+
+    for ext in VOICE_LIB_EXTS:
+        target_name = f"{safe_name}{ext}".lower()
+        for path in library_dir.iterdir():
+            if path.is_file() and path.name.lower() == target_name:
+                return path
+
+    return None
+
+
+def _delete_existing_voice_files(safe_name: str, keep: Optional[Path] = None) -> None:
+    keep_resolved = keep.resolve(strict=False) if keep is not None else None
+    for ext in VOICE_LIB_EXTS:
+        path = _voice_path(safe_name, ext)
+        if keep_resolved is not None and path.resolve(strict=False) == keep_resolved:
+            continue
+        try:
+            path.unlink()
+        except FileNotFoundError:
+            continue
+
+
+def _format_voice_path(path: Path) -> str:
+    return path.as_posix()

codex
有一个残留 `git` 进程仍在后台，应该是刚才超时的推送。我会结束它，避免留下挂起的认证进程。
exec
"C:\\Program Files\\PowerShell\\7\\pwsh.exe" -Command 'Stop-Process -Id 22728 -Force; Get-Process git -ErrorAction SilentlyContinue | Select-Object Id,ProcessName,StartTime' in D:\apiWorkSpace\index-tts2-vLLM
2026-05-24T17:53:01.336629Z ERROR codex_core::tools::router: error=Exit code: 1
Wall time: 0.5 seconds
Output:
[31;1mStop-Process: [0m
[31;1m[36;1mLine |[0m
[31;1m[36;1m[36;1m   2 | [0m [36;1mStop-Process -Id 22728 -Force[0m; Get-Process git -ErrorAction SilentlyC …[0m
[31;1m[36;1m[36;1m[0m[36;1m[0m[36;1m     | [31;1m ~~~~~~~~~~~~~~~~~~~~~~~~~~~~~[0m
[31;1m[36;1m[36;1m[0m[36;1m[0m[36;1m[31;1m[31;1m[36;1m     | [31;1mCannot find a process with the process identifier 22728.[0m

 exited 1 in 540ms:
[31;1mStop-Process: [0m
[31;1m[36;1mLine |[0m
[31;1m[36;1m[36;1m   2 | [0m [36;1mStop-Process -Id 22728 -Force[0m; Get-Process git -ErrorAction SilentlyC …[0m
[31;1m[36;1m[36;1m[0m[36;1m[0m[36;1m     | [31;1m ~~~~~~~~~~~~~~~~~~~~~~~~~~~~~[0m
[31;1m[36;1m[36;1m[0m[36;1m[0m[36;1m[31;1m[31;1m[36;1m     | [31;1mCannot find a process with the process identifier 22728.[0m

diff --git a/indextts/voice_library.py b/indextts/voice_library.py
new file mode 100644
index 0000000000000000000000000000000000000000..4ee5d9b8f5f16a07feb288be39cfcdcdd438b696
--- /dev/null
+++ b/indextts/voice_library.py
@@ -0,0 +1,164 @@
+import re
+import shutil
+from pathlib import Path
+from typing import Optional
+
+
+VOICE_LIB_DIR = "prompts/library"
+VOICE_LIB_EXTS = (".wav", ".mp3", ".flac", ".ogg", ".m4a")
+
+_INVALID_NAME_CHARS = re.compile(r'[\\/:*?"<>|\x00-\x1f]')
+
+
+def safe_voice_name(name: str) -> str:
+    """Return a Windows-safe voice filename stem capped at 60 chars."""
+    cleaned = _INVALID_NAME_CHARS.sub("", (name or "").strip())
+    return cleaned[:60]
+
+
+def list_voices() -> list[dict]:
+    """List saved voices with name, relative path, extension, and byte size."""
+    library_dir = Path(VOICE_LIB_DIR)
+    if not library_dir.is_dir():
+        return []
+
+    items = []
+    for path in library_dir.iterdir():
+        if not path.is_file():
+            continue
+
+        ext = path.suffix.lower()
+        if ext not in VOICE_LIB_EXTS:
+            continue
+
+        try:
+            size_bytes = path.stat().st_size
+        except OSError:
+            continue
+
+        items.append(
+            {
+                "name": path.stem,
+                "path": _format_voice_path(path),
+                "ext": ext,
+                "size_bytes": size_bytes,
+            }
+        )
+
+    items.sort(key=lambda item: (item["name"].lower(), item["name"], item["ext"]))
+    return items
+
+
+def get_voice_path(name: str) -> Optional[str]:
+    """Return a saved voice path by name, or None when it does not exist."""
+    safe = safe_voice_name(name)
+    if not safe:
+        return None
+
+    path = _find_voice_path(safe)
+    if path is None:
+        return None
+    return _format_voice_path(path)
+
+
+def save_voice(audio_bytes: bytes, name: str, ext: str = ".wav") -> str:
+    """Save uploaded audio bytes and return the final voice file path."""
+    safe = safe_voice_name(name)
+    if not safe:
+        raise ValueError("voice name is empty")
+
+    normalized_ext = _normalize_voice_ext(ext)
+    dst = _voice_path(safe, normalized_ext)
+    dst.parent.mkdir(parents=True, exist_ok=True)
+    _delete_existing_voice_files(safe, keep=dst)
+
+    with dst.open("wb") as file:
+        file.write(audio_bytes)
+
+    return _format_voice_path(dst)
+
+
+def save_voice_from_path(source_path: str, name: str) -> str:
+    """Copy a local audio file into the voice library and return its path."""
+    safe = safe_voice_name(name)
+    if not safe:
+        raise ValueError("voice name is empty")
+
+    source = Path(source_path)
+    if not source.is_file():
+        raise FileNotFoundError(source_path)
+
+    normalized_ext = _normalize_voice_ext(source.suffix)
+    dst = _voice_path(safe, normalized_ext)
+    dst.parent.mkdir(parents=True, exist_ok=True)
+    _delete_existing_voice_files(safe, keep=dst)
+
+    if source.resolve(strict=False) != dst.resolve(strict=False):
+        shutil.copy2(source, dst)
+
+    return _format_voice_path(dst)
+
+
+def delete_voice(name: str) -> bool:
+    """Delete a saved voice by name, returning False when absent or failed."""
+    safe = safe_voice_name(name)
+    if not safe:
+        return False
+
+    path = _find_voice_path(safe)
+    if path is None:
+        return False
+
+    try:
+        path.unlink()
+    except OSError:
+        return False
+    return True
+
+
+def _normalize_voice_ext(ext: str) -> str:
+    ext = (ext or ".wav").lower()
+    if not ext.startswith("."):
+        ext = "." + ext
+    if ext not in VOICE_LIB_EXTS:
+        return ".wav"
+    return ext
+
+
+def _voice_path(name: str, ext: str) -> Path:
+    return Path(VOICE_LIB_DIR) / f"{name}{ext}"
+
+
+def _find_voice_path(safe_name: str) -> Optional[Path]:
+    library_dir = Path(VOICE_LIB_DIR)
+    for ext in VOICE_LIB_EXTS:
+        path = library_dir / f"{safe_name}{ext}"
+        if path.is_file():
+            return path
+
+    if not library_dir.is_dir():
+        return None
+
+    for ext in VOICE_LIB_EXTS:
+        target_name = f"{safe_name}{ext}".lower()
+        for path in library_dir.iterdir():
+            if path.is_file() and path.name.lower() == target_name:
+                return path
+
+    return None
+
+
+def _delete_existing_voice_files(safe_name: str, keep: Optional[Path] = None) -> None:
+    keep_resolved = keep.resolve(strict=False) if keep is not None else None
+    for ext in VOICE_LIB_EXTS:
+        path = _voice_path(safe_name, ext)
+        if keep_resolved is not None and path.resolve(strict=False) == keep_resolved:
+            continue
+        try:
+            path.unlink()
+        except FileNotFoundError:
+            continue
+
+
+def _format_voice_path(path: Path) -> str:
+    return path.as_posix()

exec
"C:\\Program Files\\PowerShell\\7\\pwsh.exe" -Command 'Get-Process git -ErrorAction SilentlyContinue | Select-Object Id,ProcessName,StartTime' in D:\apiWorkSpace\index-tts2-vLLM
exec
"C:\\Program Files\\PowerShell\\7\\pwsh.exe" -Command 'git status --short --branch' in D:\apiWorkSpace\index-tts2-vLLM
 succeeded in 469ms:
## VLLM...origin/VLLM [ahead 1]
?? .claude/
?? .codex_dispatch_voice_library.log
?? "\351\237\263\350\211\262\345\217\202\350\200\203\351\237\263\351\242\221/"

 succeeded in 487ms:

[32;1m   Id[0m[32;1m ProcessName[0m[32;1m StartTime[0m
[32;1m   --[0m [32;1m-----------[0m [32;1m---------[0m
24596 git         2026/5/25 2:00:07


diff --git a/indextts/voice_library.py b/indextts/voice_library.py
new file mode 100644
index 0000000000000000000000000000000000000000..4ee5d9b8f5f16a07feb288be39cfcdcdd438b696
--- /dev/null
+++ b/indextts/voice_library.py
@@ -0,0 +1,164 @@
+import re
+import shutil
+from pathlib import Path
+from typing import Optional
+
+
+VOICE_LIB_DIR = "prompts/library"
+VOICE_LIB_EXTS = (".wav", ".mp3", ".flac", ".ogg", ".m4a")
+
+_INVALID_NAME_CHARS = re.compile(r'[\\/:*?"<>|\x00-\x1f]')
+
+
+def safe_voice_name(name: str) -> str:
+    """Return a Windows-safe voice filename stem capped at 60 chars."""
+    cleaned = _INVALID_NAME_CHARS.sub("", (name or "").strip())
+    return cleaned[:60]
+
+
+def list_voices() -> list[dict]:
+    """List saved voices with name, relative path, extension, and byte size."""
+    library_dir = Path(VOICE_LIB_DIR)
+    if not library_dir.is_dir():
+        return []
+
+    items = []
+    for path in library_dir.iterdir():
+        if not path.is_file():
+            continue
+
+        ext = path.suffix.lower()
+        if ext not in VOICE_LIB_EXTS:
+            continue
+
+        try:
+            size_bytes = path.stat().st_size
+        except OSError:
+            continue
+
+        items.append(
+            {
+                "name": path.stem,
+                "path": _format_voice_path(path),
+                "ext": ext,
+                "size_bytes": size_bytes,
+            }
+        )
+
+    items.sort(key=lambda item: (item["name"].lower(), item["name"], item["ext"]))
+    return items
+
+
+def get_voice_path(name: str) -> Optional[str]:
+    """Return a saved voice path by name, or None when it does not exist."""
+    safe = safe_voice_name(name)
+    if not safe:
+        return None
+
+    path = _find_voice_path(safe)
+    if path is None:
+        return None
+    return _format_voice_path(path)
+
+
+def save_voice(audio_bytes: bytes, name: str, ext: str = ".wav") -> str:
+    """Save uploaded audio bytes and return the final voice file path."""
+    safe = safe_voice_name(name)
+    if not safe:
+        raise ValueError("voice name is empty")
+
+    normalized_ext = _normalize_voice_ext(ext)
+    dst = _voice_path(safe, normalized_ext)
+    dst.parent.mkdir(parents=True, exist_ok=True)
+    _delete_existing_voice_files(safe, keep=dst)
+
+    with dst.open("wb") as file:
+        file.write(audio_bytes)
+
+    return _format_voice_path(dst)
+
+
+def save_voice_from_path(source_path: str, name: str) -> str:
+    """Copy a local audio file into the voice library and return its path."""
+    safe = safe_voice_name(name)
+    if not safe:
+        raise ValueError("voice name is empty")
+
+    source = Path(source_path)
+    if not source.is_file():
+        raise FileNotFoundError(source_path)
+
+    normalized_ext = _normalize_voice_ext(source.suffix)
+    dst = _voice_path(safe, normalized_ext)
+    dst.parent.mkdir(parents=True, exist_ok=True)
+    _delete_existing_voice_files(safe, keep=dst)
+
+    if source.resolve(strict=False) != dst.resolve(strict=False):
+        shutil.copy2(source, dst)
+
+    return _format_voice_path(dst)
+
+
+def delete_voice(name: str) -> bool:
+    """Delete a saved voice by name, returning False when absent or failed."""
+    safe = safe_voice_name(name)
+    if not safe:
+        return False
+
+    path = _find_voice_path(safe)
+    if path is None:
+        return False
+
+    try:
+        path.unlink()
+    except OSError:
+        return False
+    return True
+
+
+def _normalize_voice_ext(ext: str) -> str:
+    ext = (ext or ".wav").lower()
+    if not ext.startswith("."):
+        ext = "." + ext
+    if ext not in VOICE_LIB_EXTS:
+        return ".wav"
+    return ext
+
+
+def _voice_path(name: str, ext: str) -> Path:
+    return Path(VOICE_LIB_DIR) / f"{name}{ext}"
+
+
+def _find_voice_path(safe_name: str) -> Optional[Path]:
+    library_dir = Path(VOICE_LIB_DIR)
+    for ext in VOICE_LIB_EXTS:
+        path = library_dir / f"{safe_name}{ext}"
+        if path.is_file():
+            return path
+
+    if not library_dir.is_dir():
+        return None
+
+    for ext in VOICE_LIB_EXTS:
+        target_name = f"{safe_name}{ext}".lower()
+        for path in library_dir.iterdir():
+            if path.is_file() and path.name.lower() == target_name:
+                return path
+
+    return None
+
+
+def _delete_existing_voice_files(safe_name: str, keep: Optional[Path] = None) -> None:
+    keep_resolved = keep.resolve(strict=False) if keep is not None else None
+    for ext in VOICE_LIB_EXTS:
+        path = _voice_path(safe_name, ext)
+        if keep_resolved is not None and path.resolve(strict=False) == keep_resolved:
+            continue
+        try:
+            path.unlink()
+        except FileNotFoundError:
+            continue
+
+
+def _format_voice_path(path: Path) -> str:
+    return path.as_posix()

exec
"C:\\Program Files\\PowerShell\\7\\pwsh.exe" -Command 'Get-CimInstance Win32_Process -Filter "ProcessId = 24596" | Select-Object ProcessId,CommandLine' in D:\apiWorkSpace\index-tts2-vLLM
2026-05-24T18:00:12.001181Z ERROR codex_core::tools::router: error=Exit code: 1
Wall time: 0.5 seconds
Output:
[31;1mGet-CimInstance: [0m
[31;1m[36;1mLine |[0m
[31;1m[36;1m[36;1m   2 | [0m [36;1mGet-CimInstance Win32_Process -Filter "ProcessId = 24596"[0m | Select-Ob …[0m
[31;1m[36;1m[36;1m[0m[36;1m[0m[36;1m     | [31;1m ~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~[0m
[31;1m[36;1m[36;1m[0m[36;1m[0m[36;1m[31;1m[31;1m[36;1m     | [31;1m无法从客户端中访问 CIM 资源。[0m

 exited 1 in 548ms:
[31;1mGet-CimInstance: [0m
[31;1m[36;1mLine |[0m
[31;1m[36;1m[36;1m   2 | [0m [36;1mGet-CimInstance Win32_Process -Filter "ProcessId = 24596"[0m | Select-Ob …[0m
[31;1m[36;1m[36;1m[0m[36;1m[0m[36;1m     | [31;1m ~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~[0m
[31;1m[36;1m[36;1m[0m[36;1m[0m[36;1m[31;1m[31;1m[36;1m     | [31;1m无法从客户端中访问 CIM 资源。[0m

diff --git a/indextts/voice_library.py b/indextts/voice_library.py
new file mode 100644
index 0000000000000000000000000000000000000000..4ee5d9b8f5f16a07feb288be39cfcdcdd438b696
--- /dev/null
+++ b/indextts/voice_library.py
@@ -0,0 +1,164 @@
+import re
+import shutil
+from pathlib import Path
+from typing import Optional
+
+
+VOICE_LIB_DIR = "prompts/library"
+VOICE_LIB_EXTS = (".wav", ".mp3", ".flac", ".ogg", ".m4a")
+
+_INVALID_NAME_CHARS = re.compile(r'[\\/:*?"<>|\x00-\x1f]')
+
+
+def safe_voice_name(name: str) -> str:
+    """Return a Windows-safe voice filename stem capped at 60 chars."""
+    cleaned = _INVALID_NAME_CHARS.sub("", (name or "").strip())
+    return cleaned[:60]
+
+
+def list_voices() -> list[dict]:
+    """List saved voices with name, relative path, extension, and byte size."""
+    library_dir = Path(VOICE_LIB_DIR)
+    if not library_dir.is_dir():
+        return []
+
+    items = []
+    for path in library_dir.iterdir():
+        if not path.is_file():
+            continue
+
+        ext = path.suffix.lower()
+        if ext not in VOICE_LIB_EXTS:
+            continue
+
+        try:
+            size_bytes = path.stat().st_size
+        except OSError:
+            continue
+
+        items.append(
+            {
+                "name": path.stem,
+                "path": _format_voice_path(path),
+                "ext": ext,
+                "size_bytes": size_bytes,
+            }
+        )
+
+    items.sort(key=lambda item: (item["name"].lower(), item["name"], item["ext"]))
+    return items
+
+
+def get_voice_path(name: str) -> Optional[str]:
+    """Return a saved voice path by name, or None when it does not exist."""
+    safe = safe_voice_name(name)
+    if not safe:
+        return None
+
+    path = _find_voice_path(safe)
+    if path is None:
+        return None
+    return _format_voice_path(path)
+
+
+def save_voice(audio_bytes: bytes, name: str, ext: str = ".wav") -> str:
+    """Save uploaded audio bytes and return the final voice file path."""
+    safe = safe_voice_name(name)
+    if not safe:
+        raise ValueError("voice name is empty")
+
+    normalized_ext = _normalize_voice_ext(ext)
+    dst = _voice_path(safe, normalized_ext)
+    dst.parent.mkdir(parents=True, exist_ok=True)
+    _delete_existing_voice_files(safe, keep=dst)
+
+    with dst.open("wb") as file:
+        file.write(audio_bytes)
+
+    return _format_voice_path(dst)
+
+
+def save_voice_from_path(source_path: str, name: str) -> str:
+    """Copy a local audio file into the voice library and return its path."""
+    safe = safe_voice_name(name)
+    if not safe:
+        raise ValueError("voice name is empty")
+
+    source = Path(source_path)
+    if not source.is_file():
+        raise FileNotFoundError(source_path)
+
+    normalized_ext = _normalize_voice_ext(source.suffix)
+    dst = _voice_path(safe, normalized_ext)
+    dst.parent.mkdir(parents=True, exist_ok=True)
+    _delete_existing_voice_files(safe, keep=dst)
+
+    if source.resolve(strict=False) != dst.resolve(strict=False):
+        shutil.copy2(source, dst)
+
+    return _format_voice_path(dst)
+
+
+def delete_voice(name: str) -> bool:
+    """Delete a saved voice by name, returning False when absent or failed."""
+    safe = safe_voice_name(name)
+    if not safe:
+        return False
+
+    path = _find_voice_path(safe)
+    if path is None:
+        return False
+
+    try:
+        path.unlink()
+    except OSError:
+        return False
+    return True
+
+
+def _normalize_voice_ext(ext: str) -> str:
+    ext = (ext or ".wav").lower()
+    if not ext.startswith("."):
+        ext = "." + ext
+    if ext not in VOICE_LIB_EXTS:
+        return ".wav"
+    return ext
+
+
+def _voice_path(name: str, ext: str) -> Path:
+    return Path(VOICE_LIB_DIR) / f"{name}{ext}"
+
+
+def _find_voice_path(safe_name: str) -> Optional[Path]:
+    library_dir = Path(VOICE_LIB_DIR)
+    for ext in VOICE_LIB_EXTS:
+        path = library_dir / f"{safe_name}{ext}"
+        if path.is_file():
+            return path
+
+    if not library_dir.is_dir():
+        return None
+
+    for ext in VOICE_LIB_EXTS:
+        target_name = f"{safe_name}{ext}".lower()
+        for path in library_dir.iterdir():
+            if path.is_file() and path.name.lower() == target_name:
+                return path
+
+    return None
+
+
+def _delete_existing_voice_files(safe_name: str, keep: Optional[Path] = None) -> None:
+    keep_resolved = keep.resolve(strict=False) if keep is not None else None
+    for ext in VOICE_LIB_EXTS:
+        path = _voice_path(safe_name, ext)
+        if keep_resolved is not None and path.resolve(strict=False) == keep_resolved:
+            continue
+        try:
+            path.unlink()
+        except FileNotFoundError:
+            continue
+
+
+def _format_voice_path(path: Path) -> str:
+    return path.as_posix()

exec
"C:\\Program Files\\PowerShell\\7\\pwsh.exe" -Command 'Stop-Process -Id 24596 -Force -ErrorAction SilentlyContinue; Start-Sleep -Seconds 1; Get-Process git -ErrorAction SilentlyContinue | Select-Object Id,ProcessName,StartTime' in D:\apiWorkSpace\index-tts2-vLLM
2026-05-24T18:00:19.169417Z ERROR codex_core::tools::router: error=Exit code: 1
Wall time: 1.4 seconds
Output:

 exited 1 in 1448ms:
diff --git a/indextts/voice_library.py b/indextts/voice_library.py
new file mode 100644
index 0000000000000000000000000000000000000000..4ee5d9b8f5f16a07feb288be39cfcdcdd438b696
--- /dev/null
+++ b/indextts/voice_library.py
@@ -0,0 +1,164 @@
+import re
+import shutil
+from pathlib import Path
+from typing import Optional
+
+
+VOICE_LIB_DIR = "prompts/library"
+VOICE_LIB_EXTS = (".wav", ".mp3", ".flac", ".ogg", ".m4a")
+
+_INVALID_NAME_CHARS = re.compile(r'[\\/:*?"<>|\x00-\x1f]')
+
+
+def safe_voice_name(name: str) -> str:
+    """Return a Windows-safe voice filename stem capped at 60 chars."""
+    cleaned = _INVALID_NAME_CHARS.sub("", (name or "").strip())
+    return cleaned[:60]
+
+
+def list_voices() -> list[dict]:
+    """List saved voices with name, relative path, extension, and byte size."""
+    library_dir = Path(VOICE_LIB_DIR)
+    if not library_dir.is_dir():
+        return []
+
+    items = []
+    for path in library_dir.iterdir():
+        if not path.is_file():
+            continue
+
+        ext = path.suffix.lower()
+        if ext not in VOICE_LIB_EXTS:
+            continue
+
+        try:
+            size_bytes = path.stat().st_size
+        except OSError:
+            continue
+
+        items.append(
+            {
+                "name": path.stem,
+                "path": _format_voice_path(path),
+                "ext": ext,
+                "size_bytes": size_bytes,
+            }
+        )
+
+    items.sort(key=lambda item: (item["name"].lower(), item["name"], item["ext"]))
+    return items
+
+
+def get_voice_path(name: str) -> Optional[str]:
+    """Return a saved voice path by name, or None when it does not exist."""
+    safe = safe_voice_name(name)
+    if not safe:
+        return None
+
+    path = _find_voice_path(safe)
+    if path is None:
+        return None
+    return _format_voice_path(path)
+
+
+def save_voice(audio_bytes: bytes, name: str, ext: str = ".wav") -> str:
+    """Save uploaded audio bytes and return the final voice file path."""
+    safe = safe_voice_name(name)
+    if not safe:
+        raise ValueError("voice name is empty")
+
+    normalized_ext = _normalize_voice_ext(ext)
+    dst = _voice_path(safe, normalized_ext)
+    dst.parent.mkdir(parents=True, exist_ok=True)
+    _delete_existing_voice_files(safe, keep=dst)
+
+    with dst.open("wb") as file:
+        file.write(audio_bytes)
+
+    return _format_voice_path(dst)
+
+
+def save_voice_from_path(source_path: str, name: str) -> str:
+    """Copy a local audio file into the voice library and return its path."""
+    safe = safe_voice_name(name)
+    if not safe:
+        raise ValueError("voice name is empty")
+
+    source = Path(source_path)
+    if not source.is_file():
+        raise FileNotFoundError(source_path)
+
+    normalized_ext = _normalize_voice_ext(source.suffix)
+    dst = _voice_path(safe, normalized_ext)
+    dst.parent.mkdir(parents=True, exist_ok=True)
+    _delete_existing_voice_files(safe, keep=dst)
+
+    if source.resolve(strict=False) != dst.resolve(strict=False):
+        shutil.copy2(source, dst)
+
+    return _format_voice_path(dst)
+
+
+def delete_voice(name: str) -> bool:
+    """Delete a saved voice by name, returning False when absent or failed."""
+    safe = safe_voice_name(name)
+    if not safe:
+        return False
+
+    path = _find_voice_path(safe)
+    if path is None:
+        return False
+
+    try:
+        path.unlink()
+    except OSError:
+        return False
+    return True
+
+
+def _normalize_voice_ext(ext: str) -> str:
+    ext = (ext or ".wav").lower()
+    if not ext.startswith("."):
+        ext = "." + ext
+    if ext not in VOICE_LIB_EXTS:
+        return ".wav"
+    return ext
+
+
+def _voice_path(name: str, ext: str) -> Path:
+    return Path(VOICE_LIB_DIR) / f"{name}{ext}"
+
+
+def _find_voice_path(safe_name: str) -> Optional[Path]:
+    library_dir = Path(VOICE_LIB_DIR)
+    for ext in VOICE_LIB_EXTS:
+        path = library_dir / f"{safe_name}{ext}"
+        if path.is_file():
+            return path
+
+    if not library_dir.is_dir():
+        return None
+
+    for ext in VOICE_LIB_EXTS:
+        target_name = f"{safe_name}{ext}".lower()
+        for path in library_dir.iterdir():
+            if path.is_file() and path.name.lower() == target_name:
+                return path
+
+    return None
+
+
+def _delete_existing_voice_files(safe_name: str, keep: Optional[Path] = None) -> None:
+    keep_resolved = keep.resolve(strict=False) if keep is not None else None
+    for ext in VOICE_LIB_EXTS:
+        path = _voice_path(safe_name, ext)
+        if keep_resolved is not None and path.resolve(strict=False) == keep_resolved:
+            continue
+        try:
+            path.unlink()
+        except FileNotFoundError:
+            continue
+
+
+def _format_voice_path(path: Path) -> str:
+    return path.as_posix()

codex
残留进程已经清理。最后再用禁用交互提示的方式推一次，以拿到明确失败原因；如果还是缺凭据，就只能保留本地 commit。
exec
"C:\\Program Files\\PowerShell\\7\\pwsh.exe" -Command '$env:GIT_TERMINAL_PROMPT='"'0'; git -c http.sslBackend=openssl push userrepo VLLM" in D:\apiWorkSpace\index-tts2-vLLM
2026-05-24T18:02:28.260254Z ERROR codex_core::tools::router: error=Exit code: 124
Wall time: 120.2 seconds
Output:
command timed out after 120162 milliseconds

 exited 124 in 120162ms:
diff --git a/indextts/voice_library.py b/indextts/voice_library.py
new file mode 100644
index 0000000000000000000000000000000000000000..4ee5d9b8f5f16a07feb288be39cfcdcdd438b696
--- /dev/null
+++ b/indextts/voice_library.py
@@ -0,0 +1,164 @@
+import re
+import shutil
+from pathlib import Path
+from typing import Optional
+
+
+VOICE_LIB_DIR = "prompts/library"
+VOICE_LIB_EXTS = (".wav", ".mp3", ".flac", ".ogg", ".m4a")
+
+_INVALID_NAME_CHARS = re.compile(r'[\\/:*?"<>|\x00-\x1f]')
+
+
+def safe_voice_name(name: str) -> str:
+    """Return a Windows-safe voice filename stem capped at 60 chars."""
+    cleaned = _INVALID_NAME_CHARS.sub("", (name or "").strip())
+    return cleaned[:60]
+
+
+def list_voices() -> list[dict]:
+    """List saved voices with name, relative path, extension, and byte size."""
+    library_dir = Path(VOICE_LIB_DIR)
+    if not library_dir.is_dir():
+        return []
+
+    items = []
+    for path in library_dir.iterdir():
+        if not path.is_file():
+            continue
+
+        ext = path.suffix.lower()
+        if ext not in VOICE_LIB_EXTS:
+            continue
+
+        try:
+            size_bytes = path.stat().st_size
+        except OSError:
+            continue
+
+        items.append(
+            {
+                "name": path.stem,
+                "path": _format_voice_path(path),
+                "ext": ext,
+                "size_bytes": size_bytes,
+            }
+        )
+
+    items.sort(key=lambda item: (item["name"].lower(), item["name"], item["ext"]))
+    return items
+
+
+def get_voice_path(name: str) -> Optional[str]:
+    """Return a saved voice path by name, or None when it does not exist."""
+    safe = safe_voice_name(name)
+    if not safe:
+        return None
+
+    path = _find_voice_path(safe)
+    if path is None:
+        return None
+    return _format_voice_path(path)
+
+
+def save_voice(audio_bytes: bytes, name: str, ext: str = ".wav") -> str:
+    """Save uploaded audio bytes and return the final voice file path."""
+    safe = safe_voice_name(name)
+    if not safe:
+        raise ValueError("voice name is empty")
+
+    normalized_ext = _normalize_voice_ext(ext)
+    dst = _voice_path(safe, normalized_ext)
+    dst.parent.mkdir(parents=True, exist_ok=True)
+    _delete_existing_voice_files(safe, keep=dst)
+
+    with dst.open("wb") as file:
+        file.write(audio_bytes)
+
+    return _format_voice_path(dst)
+
+
+def save_voice_from_path(source_path: str, name: str) -> str:
+    """Copy a local audio file into the voice library and return its path."""
+    safe = safe_voice_name(name)
+    if not safe:
+        raise ValueError("voice name is empty")
+
+    source = Path(source_path)
+    if not source.is_file():
+        raise FileNotFoundError(source_path)
+
+    normalized_ext = _normalize_voice_ext(source.suffix)
+    dst = _voice_path(safe, normalized_ext)
+    dst.parent.mkdir(parents=True, exist_ok=True)
+    _delete_existing_voice_files(safe, keep=dst)
+
+    if source.resolve(strict=False) != dst.resolve(strict=False):
+        shutil.copy2(source, dst)
+
+    return _format_voice_path(dst)
+
+
+def delete_voice(name: str) -> bool:
+    """Delete a saved voice by name, returning False when absent or failed."""
+    safe = safe_voice_name(name)
+    if not safe:
+        return False
+
+    path = _find_voice_path(safe)
+    if path is None:
+        return False
+
+    try:
+        path.unlink()
+    except OSError:
+        return False
+    return True
+
+
+def _normalize_voice_ext(ext: str) -> str:
+    ext = (ext or ".wav").lower()
+    if not ext.startswith("."):
+        ext = "." + ext
+    if ext not in VOICE_LIB_EXTS:
+        return ".wav"
+    return ext
+
+
+def _voice_path(name: str, ext: str) -> Path:
+    return Path(VOICE_LIB_DIR) / f"{name}{ext}"
+
+
+def _find_voice_path(safe_name: str) -> Optional[Path]:
+    library_dir = Path(VOICE_LIB_DIR)
+    for ext in VOICE_LIB_EXTS:
+        path = library_dir / f"{safe_name}{ext}"
+        if path.is_file():
+            return path
+
+    if not library_dir.is_dir():
+        return None
+
+    for ext in VOICE_LIB_EXTS:
+        target_name = f"{safe_name}{ext}".lower()
+        for path in library_dir.iterdir():
+            if path.is_file() and path.name.lower() == target_name:
+                return path
+
+    return None
+
+
+def _delete_existing_voice_files(safe_name: str, keep: Optional[Path] = None) -> None:
+    keep_resolved = keep.resolve(strict=False) if keep is not None else None
+    for ext in VOICE_LIB_EXTS:
+        path = _voice_path(safe_name, ext)
+        if keep_resolved is not None and path.resolve(strict=False) == keep_resolved:
+            continue
+        try:
+            path.unlink()
+        except FileNotFoundError:
+            continue
+
+
+def _format_voice_path(path: Path) -> str:
+    return path.as_posix()

exec
"C:\\Program Files\\PowerShell\\7\\pwsh.exe" -Command 'Get-Process git -ErrorAction SilentlyContinue | Select-Object Id,ProcessName,StartTime' in D:\apiWorkSpace\index-tts2-vLLM
exec
"C:\\Program Files\\PowerShell\\7\\pwsh.exe" -Command 'git status --short --branch' in D:\apiWorkSpace\index-tts2-vLLM
2026-05-24T18:02:35.060842Z ERROR codex_core::tools::router: error=Exit code: 1
Wall time: 0.5 seconds
Output:

 exited 1 in 464ms:
 succeeded in 427ms:
## VLLM...origin/VLLM [ahead 1]
?? .claude/
?? .codex_dispatch_voice_library.log
?? "\351\237\263\350\211\262\345\217\202\350\200\203\351\237\263\351\242\221/"

diff --git a/indextts/voice_library.py b/indextts/voice_library.py
new file mode 100644
index 0000000000000000000000000000000000000000..4ee5d9b8f5f16a07feb288be39cfcdcdd438b696
--- /dev/null
+++ b/indextts/voice_library.py
@@ -0,0 +1,164 @@
+import re
+import shutil
+from pathlib import Path
+from typing import Optional
+
+
+VOICE_LIB_DIR = "prompts/library"
+VOICE_LIB_EXTS = (".wav", ".mp3", ".flac", ".ogg", ".m4a")
+
+_INVALID_NAME_CHARS = re.compile(r'[\\/:*?"<>|\x00-\x1f]')
+
+
+def safe_voice_name(name: str) -> str:
+    """Return a Windows-safe voice filename stem capped at 60 chars."""
+    cleaned = _INVALID_NAME_CHARS.sub("", (name or "").strip())
+    return cleaned[:60]
+
+
+def list_voices() -> list[dict]:
+    """List saved voices with name, relative path, extension, and byte size."""
+    library_dir = Path(VOICE_LIB_DIR)
+    if not library_dir.is_dir():
+        return []
+
+    items = []
+    for path in library_dir.iterdir():
+        if not path.is_file():
+            continue
+
+        ext = path.suffix.lower()
+        if ext not in VOICE_LIB_EXTS:
+            continue
+
+        try:
+            size_bytes = path.stat().st_size
+        except OSError:
+            continue
+
+        items.append(
+            {
+                "name": path.stem,
+                "path": _format_voice_path(path),
+                "ext": ext,
+                "size_bytes": size_bytes,
+            }
+        )
+
+    items.sort(key=lambda item: (item["name"].lower(), item["name"], item["ext"]))
+    return items
+
+
+def get_voice_path(name: str) -> Optional[str]:
+    """Return a saved voice path by name, or None when it does not exist."""
+    safe = safe_voice_name(name)
+    if not safe:
+        return None
+
+    path = _find_voice_path(safe)
+    if path is None:
+        return None
+    return _format_voice_path(path)
+
+
+def save_voice(audio_bytes: bytes, name: str, ext: str = ".wav") -> str:
+    """Save uploaded audio bytes and return the final voice file path."""
+    safe = safe_voice_name(name)
+    if not safe:
+        raise ValueError("voice name is empty")
+
+    normalized_ext = _normalize_voice_ext(ext)
+    dst = _voice_path(safe, normalized_ext)
+    dst.parent.mkdir(parents=True, exist_ok=True)
+    _delete_existing_voice_files(safe, keep=dst)
+
+    with dst.open("wb") as file:
+        file.write(audio_bytes)
+
+    return _format_voice_path(dst)
+
+
+def save_voice_from_path(source_path: str, name: str) -> str:
+    """Copy a local audio file into the voice library and return its path."""
+    safe = safe_voice_name(name)
+    if not safe:
+        raise ValueError("voice name is empty")
+
+    source = Path(source_path)
+    if not source.is_file():
+        raise FileNotFoundError(source_path)
+
+    normalized_ext = _normalize_voice_ext(source.suffix)
+    dst = _voice_path(safe, normalized_ext)
+    dst.parent.mkdir(parents=True, exist_ok=True)
+    _delete_existing_voice_files(safe, keep=dst)
+
+    if source.resolve(strict=False) != dst.resolve(strict=False):
+        shutil.copy2(source, dst)
+
+    return _format_voice_path(dst)
+
+
+def delete_voice(name: str) -> bool:
+    """Delete a saved voice by name, returning False when absent or failed."""
+    safe = safe_voice_name(name)
+    if not safe:
+        return False
+
+    path = _find_voice_path(safe)
+    if path is None:
+        return False
+
+    try:
+        path.unlink()
+    except OSError:
+        return False
+    return True
+
+
+def _normalize_voice_ext(ext: str) -> str:
+    ext = (ext or ".wav").lower()
+    if not ext.startswith("."):
+        ext = "." + ext
+    if ext not in VOICE_LIB_EXTS:
+        return ".wav"
+    return ext
+
+
+def _voice_path(name: str, ext: str) -> Path:
+    return Path(VOICE_LIB_DIR) / f"{name}{ext}"
+
+
+def _find_voice_path(safe_name: str) -> Optional[Path]:
+    library_dir = Path(VOICE_LIB_DIR)
+    for ext in VOICE_LIB_EXTS:
+        path = library_dir / f"{safe_name}{ext}"
+        if path.is_file():
+            return path
+
+    if not library_dir.is_dir():
+        return None
+
+    for ext in VOICE_LIB_EXTS:
+        target_name = f"{safe_name}{ext}".lower()
+        for path in library_dir.iterdir():
+            if path.is_file() and path.name.lower() == target_name:
+                return path
+
+    return None
+
+
+def _delete_existing_voice_files(safe_name: str, keep: Optional[Path] = None) -> None:
+    keep_resolved = keep.resolve(strict=False) if keep is not None else None
+    for ext in VOICE_LIB_EXTS:
+        path = _voice_path(safe_name, ext)
+        if keep_resolved is not None and path.resolve(strict=False) == keep_resolved:
+            continue
+        try:
+            path.unlink()
+        except FileNotFoundError:
+            continue
+
+
+def _format_voice_path(path: Path) -> str:
+    return path.as_posix()

codex
已新增并提交 `indextts/voice_library.py`，语法检查通过；推送 `userrepo VLLM` 因 HTTPS 凭据/网络超时未完成。  
commit SHA: `353febc`
diff --git a/indextts/voice_library.py b/indextts/voice_library.py
new file mode 100644
index 0000000000000000000000000000000000000000..4ee5d9b8f5f16a07feb288be39cfcdcdd438b696
--- /dev/null
+++ b/indextts/voice_library.py
@@ -0,0 +1,164 @@
+import re
+import shutil
+from pathlib import Path
+from typing import Optional
+
+
+VOICE_LIB_DIR = "prompts/library"
+VOICE_LIB_EXTS = (".wav", ".mp3", ".flac", ".ogg", ".m4a")
+
+_INVALID_NAME_CHARS = re.compile(r'[\\/:*?"<>|\x00-\x1f]')
+
+
+def safe_voice_name(name: str) -> str:
+    """Return a Windows-safe voice filename stem capped at 60 chars."""
+    cleaned = _INVALID_NAME_CHARS.sub("", (name or "").strip())
+    return cleaned[:60]
+
+
+def list_voices() -> list[dict]:
+    """List saved voices with name, relative path, extension, and byte size."""
+    library_dir = Path(VOICE_LIB_DIR)
+    if not library_dir.is_dir():
+        return []
+
+    items = []
+    for path in library_dir.iterdir():
+        if not path.is_file():
+            continue
+
+        ext = path.suffix.lower()
+        if ext not in VOICE_LIB_EXTS:
+            continue
+
+        try:
+            size_bytes = path.stat().st_size
+        except OSError:
+            continue
+
+        items.append(
+            {
+                "name": path.stem,
+                "path": _format_voice_path(path),
+                "ext": ext,
+                "size_bytes": size_bytes,
+            }
+        )
+
+    items.sort(key=lambda item: (item["name"].lower(), item["name"], item["ext"]))
+    return items
+
+
+def get_voice_path(name: str) -> Optional[str]:
+    """Return a saved voice path by name, or None when it does not exist."""
+    safe = safe_voice_name(name)
+    if not safe:
+        return None
+
+    path = _find_voice_path(safe)
+    if path is None:
+        return None
+    return _format_voice_path(path)
+
+
+def save_voice(audio_bytes: bytes, name: str, ext: str = ".wav") -> str:
+    """Save uploaded audio bytes and return the final voice file path."""
+    safe = safe_voice_name(name)
+    if not safe:
+        raise ValueError("voice name is empty")
+
+    normalized_ext = _normalize_voice_ext(ext)
+    dst = _voice_path(safe, normalized_ext)
+    dst.parent.mkdir(parents=True, exist_ok=True)
+    _delete_existing_voice_files(safe, keep=dst)
+
+    with dst.open("wb") as file:
+        file.write(audio_bytes)
+
+    return _format_voice_path(dst)
+
+
+def save_voice_from_path(source_path: str, name: str) -> str:
+    """Copy a local audio file into the voice library and return its path."""
+    safe = safe_voice_name(name)
+    if not safe:
+        raise ValueError("voice name is empty")
+
+    source = Path(source_path)
+    if not source.is_file():
+        raise FileNotFoundError(source_path)
+
+    normalized_ext = _normalize_voice_ext(source.suffix)
+    dst = _voice_path(safe, normalized_ext)
+    dst.parent.mkdir(parents=True, exist_ok=True)
+    _delete_existing_voice_files(safe, keep=dst)
+
+    if source.resolve(strict=False) != dst.resolve(strict=False):
+        shutil.copy2(source, dst)
+
+    return _format_voice_path(dst)
+
+
+def delete_voice(name: str) -> bool:
+    """Delete a saved voice by name, returning False when absent or failed."""
+    safe = safe_voice_name(name)
+    if not safe:
+        return False
+
+    path = _find_voice_path(safe)
+    if path is None:
+        return False
+
+    try:
+        path.unlink()
+    except OSError:
+        return False
+    return True
+
+
+def _normalize_voice_ext(ext: str) -> str:
+    ext = (ext or ".wav").lower()
+    if not ext.startswith("."):
+        ext = "." + ext
+    if ext not in VOICE_LIB_EXTS:
+        return ".wav"
+    return ext
+
+
+def _voice_path(name: str, ext: str) -> Path:
+    return Path(VOICE_LIB_DIR) / f"{name}{ext}"
+
+
+def _find_voice_path(safe_name: str) -> Optional[Path]:
+    library_dir = Path(VOICE_LIB_DIR)
+    for ext in VOICE_LIB_EXTS:
+        path = library_dir / f"{safe_name}{ext}"
+        if path.is_file():
+            return path
+
+    if not library_dir.is_dir():
+        return None
+
+    for ext in VOICE_LIB_EXTS:
+        target_name = f"{safe_name}{ext}".lower()
+        for path in library_dir.iterdir():
+            if path.is_file() and path.name.lower() == target_name:
+                return path
+
+    return None
+
+
+def _delete_existing_voice_files(safe_name: str, keep: Optional[Path] = None) -> None:
+    keep_resolved = keep.resolve(strict=False) if keep is not None else None
+    for ext in VOICE_LIB_EXTS:
+        path = _voice_path(safe_name, ext)
+        if keep_resolved is not None and path.resolve(strict=False) == keep_resolved:
+            continue
+        try:
+            path.unlink()
+        except FileNotFoundError:
+            continue
+
+
+def _format_voice_path(path: Path) -> str:
+    return path.as_posix()

tokens used
63,901
已新增并提交 `indextts/voice_library.py`，语法检查通过；推送 `userrepo VLLM` 因 HTTPS 凭据/网络超时未完成。  
commit SHA: `353febc`
